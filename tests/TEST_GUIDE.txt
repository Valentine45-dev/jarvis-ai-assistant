JARVIS — Manual Test Guide for Round-3 Surgical Fixes
=====================================================

Covers the Round-3 fixes landed so far:
  R3-7/R3-10/R3-11/R3-12/R3-14 (0dbd82c), R3-3 (2d94d9c), R3-1/R3-2 (cd3c270),
  R3-8/R3-9 (8025da4), R3-15/R3-16/R3-17 (ecefa3f, 33771be), R3-6 (25d00b7),
  R3-13 (memory redaction), R3-20 (process-tree kill on timeout).
  (R3-18 lazy watcher / R3-19 client reuse / R3-22 external-edit reload /
   R3-21 single TTS worker-queue are internal — automated tests only.)
  (R3-4 is_speaking counter / R3-5 mute-probe are covered by automated tests
   only - tests/test_tts_is_speaking_counter.py, test_mute_probe_nonblocking.py
   - they need an audio rig / are absence-of-freeze, not practical to eyeball.)
Written for someone running the real app (uv run python main.py), not reading code.
For each fix: what to do, what you should see, and what the OLD (broken) behaviour was.

Tip: turn DEBUG on in Settings (or .env DEBUG=true) so the terminal/log shows routing
detail. Keep AUTO_CONFIRM OFF for the confirmation tests so the cards actually appear.


-----------------------------------------------------------------------
R3-12 — Vision: different questions about the SAME image now get the
        right answer (cache no longer collides on image bytes alone)
-----------------------------------------------------------------------
READ FIRST — two traps that make this look like it "doesn't work":

  (a) The line JARVIS SPEAKS FIRST is just an acknowledgement, NOT the answer.
      "Taking a look at your screen now." / "Scanning the screen for the close
      button." are the pre-analysis lines. The real answer (the description /
      where the button is) is delivered AFTER, as the spoken follow-up. To also
      see the analysis TEXT on the Terminal page, set in Settings (or
      config/jarvis.json):  vision_show_analysis = true   then re-run.

  (b) You CANNOT trigger this bug with two screenshots of the live HUD. The bug
      only fires when two captures are byte-for-byte IDENTICAL. A running JARVIS
      screen is never identical (the arc reactor pulses, the clock ticks, TTS
      animates), so two screenshots always differ, never share a cache entry,
      and the cache path is never exercised. Screenshots prove nothing here.

RELIABLE test — use the SAME IMAGE FILE twice (a file's bytes ARE identical
every time it's loaded). Pick any image on disk, e.g. a reference PNG:
     C:\Users\Lenovo\Documents\jarvis-project\assets\reference\jarvis_main_hud.png

  1. Say / type:  "describe the image at <that full path>"
        -> JARVIS describes the picture. Note the answer.
  2. Say / type:  "what colours are in the image at <that same path>"
        (a DIFFERENT question about the SAME file)

PASS (fixed):
  - The second answer is about COLOURS, not a repeat of the first description.
  - Different questions on the same file give different, on-topic answers.

FAIL (old behaviour):
  - The second question returned the FIRST question's answer verbatim, because
    the cache key was the image bytes only and ignored the question.

Cache-still-works check:
  - Ask the SAME question about the SAME file twice -> the second returns
    instantly (cache hit) with the same answer. Proves we fixed the collision
    without disabling caching.

Code-level proof (no API key, no network) — run from the repo root; this drives
the real analyze_image() with byte-identical images and a fake client:
  uv run python -c "import core.vision as v; v.clear_vision_cache(); \
R=lambda t:type('R',(),{'content':[type('X',(),{'text':t})()]})(); \
C=type('C',(),{'messages':type('M',(),{'create':lambda s,**k:R('A: '+k['messages'][0]['content'][1]['text'][:40])})()})(); \
v._get_client=lambda:C; i=b'SAME-BYTES'; \
a=v.analyze_image(i,'close button','find_ui_element'); \
b=v.analyze_image(i,'what colour is the logo','answer_question'); \
print('different question -> different answer:', a!=b, '| cache entries:', len(v._vision_cache))"
  -> expect:  different question -> different answer: True | cache entries: 2


-----------------------------------------------------------------------
R3-11 — Reminders: listing reminders while one fires no longer crashes
-----------------------------------------------------------------------
Goal: have a reminder fire at the same moment you ask to list reminders.

Do this:
  1. Set several short reminders back to back, e.g.:
        "remind me to stretch in 5 seconds"
        "remind me to drink water in 6 seconds"
        "remind me to check email in 7 seconds"
  2. As those 5-7 seconds tick down, repeatedly say / type:
        "list my reminders"
     Fire it several times quickly so one "list" lands exactly as a reminder pops.

PASS (fixed):
  - "list my reminders" always returns a clean list (or "No active reminders.").
  - Reminders fire normally on the HUD. No error toast, no broken list.

FAIL (old behaviour):
  - Occasionally the list command errored out (internally a
    "dictionary changed size during iteration" crash) right as a reminder fired,
    so you'd get a generic failure instead of the list.

Note: this is a timing/race bug, so it was intermittent before. If you can't
make it collide by hand, just confirm listing works reliably while reminders
are active — the fix removes the crash window entirely.


-----------------------------------------------------------------------
R3-10 — kill_process: JARVIS won't kill itself, matches by exact name,
        and shows the full list before a broad kill
-----------------------------------------------------------------------
JARVIS runs as python.exe, so "kill python" used to kill JARVIS itself.

TEST A — self-protection (the important one):
  Say / type:  "kill the python process"   (or "kill process named python")
  When the confirmation card appears, confirm it (say "yes").

  PASS (fixed):
    - JARVIS stays open and running.
    - If python.exe processes other than JARVIS exist, only those are reported;
      JARVIS is never in the kill list.
    - If JARVIS is the only python process, you get "No process matching 'python'"
      (it excluded itself) — and JARVIS is still alive.

  FAIL (old behaviour):
    - JARVIS abruptly closed (it killed its own process), often mid-reply.

TEST B — exact name, not substring:
  Open Notepad (notepad.exe). Then say:  "kill the process named note"
  PASS (fixed): "No process matching 'note'" — "note" is not the real name.
                Now say "kill notepad" -> Notepad closes (exact name matches).
  FAIL (old): "note" as a substring of "notepad.exe" killed Notepad.

TEST C — broad-match safety list:
  Open two or three Notepad windows (each is its own notepad.exe), then say:
        "kill notepad"
  PASS (fixed): you get a SECOND confirmation that names every match, e.g.
        "3 processes match 'notepad': notepad.exe (PID ...), notepad.exe (PID ...)
         ... Kill all of them?"
        Confirm -> all close. (A single Notepad needs no extra prompt — it just
        closes after the normal kill confirmation.)
  FAIL (old): it silently killed everything matching with no list shown.

  By PID: "kill PID <JARVIS's own pid>" -> refuses with
          "Refusing to kill JARVIS's own process."  (find the pid in Task Manager).


-----------------------------------------------------------------------
R3-7 — Confirmation: a "no / wait / cancel" reply never triggers a
        destructive action, even if it also contains "yes"
-----------------------------------------------------------------------
Use any destructive command that shows a confirmation card. A safe one to test
with is creating then deleting a throwaway file, or just "shut down the computer"
(then DON'T actually confirm — use a cancelling reply).

Do this:
  1. Say / type something that asks for confirmation, e.g.:
        "delete the file test_throwaway.txt"   (create it first with
         "create a file called test_throwaway.txt")
     A confirmation card appears.
  2. Reply with a HEDGED / self-correcting answer that contains a "yes" word
     but also a negation, e.g.:
        "yes — wait, no"
        "sure, actually cancel"
        "no don't"

PASS (fixed):
  - JARVIS stands down ("Understood — standing down.") and does NOT delete.
  - The file is still there.

FAIL (old behaviour):
  - Because the reply contained "yes"/"sure", JARVIS treated it as approval and
    DELETED the file anyway.

Also verify normal flow still works:
  - A clean "yes" / "go ahead" / "do it" still confirms and deletes.
  - A bare "please" by itself NO LONGER counts as confirmation (it's too weak) —
    reply just "please" to a card and JARVIS will NOT proceed. (As of the TEST-B
    fix it keeps the card up and re-asks "yes or no" rather than standing down,
    since "please" is ambiguous — but it never executes the action.)


-----------------------------------------------------------------------
R3-14 — Workflows: editing a fetched workflow no longer corrupts the
        saved copy
-----------------------------------------------------------------------
This guards against the library handing out a shared reference to a workflow's
steps. Hard to trigger by normal voice use, so test via behaviour stability:

Do this:
  1. Create a workflow with a few steps, e.g.:
        "create a workflow called demo that opens chrome and takes a screenshot"
  2. Run it a few times:
        "run demo"   (repeat 3-4 times)
  3. Open the AUTOMATE page and inspect the "demo" workflow's step list.
        Also open data/workflows.json in an editor.

PASS (fixed):
  - The step list for "demo" is identical every time — same steps, same order,
    same count — no matter how many times you run it or list it.
  - data/workflows.json still shows exactly the steps you defined; no duplicated,
    mutated, or extra steps appear over repeated runs.

FAIL (old behaviour):
  - Repeated runs / inspections could mutate the in-memory workflow (steps getting
    normalised or appended in place), and a later save would persist the corrupted
    version to workflows.json.

Quick confidence check: "list my workflows" several times in a row — every
listing returns the same stable data.


-----------------------------------------------------------------------
R3-3 — Raw PowerShell / CMD now asks before running (and destructive
       cmdlets are caught), instead of running unconfirmed
-----------------------------------------------------------------------
Before this fix, "use PowerShell to ..." ran immediately. PowerShell-native
destructive cmdlets (Stop-Computer, Format-Volume, ...) slipped past the danger
check entirely and could power off / wipe with NO confirmation. Now EVERY
run_powershell / run_cmd asks first.

NOTE: keep AUTO_CONFIRM off for these. Also, if you've turned on
allow_ai_command_autorun, raw shell runs without asking by design — leave it OFF
to see the confirmation behaviour.

TEST A — benign PowerShell now confirms:
  Say / type:  "use PowerShell to get the date"   (or @code Get-Date)
  PASS (fixed): a confirmation card appears ("Run this PowerShell command? ...
                Get-Date") BEFORE anything runs. Say "yes" -> it runs and shows
                the date.
  FAIL (old):   it ran immediately with no confirmation.

TEST B — destructive cmdlet is caught (the important one — do NOT confirm):
  Say / type:  "use PowerShell to run Stop-Computer"
  PASS (fixed): a confirmation card appears. Reply "no" / "cancel" -> JARVIS
                stands down and the PC stays on.
  FAIL (old):   the machine powered off with no prompt.
  ⚠ Do NOT confirm this one. Same goes for Restart-Computer, Format-Volume,
    Clear-Disk — they're now in the denylist; verify the CARD appears, then cancel.

TEST C — CMD also confirms:
  Say / type:  "use CMD to run ipconfig"
  PASS (fixed): confirmation card appears first; confirm -> it runs.

TEST D — single confirmation, not double (for a destructive one):
  "use PowerShell to run Stop-Computer" -> you should see exactly ONE card, not
  two (the fix reuses the danger-confirm gate so it doesn't prompt twice). Cancel it.

Code-level proof (no execution, no shutdown) — from repo root:
  uv run python -c "import core.handlers.code_exec as ce; \
print('benign PS confirms :', ce._handle_code_execution('run_powershell',{'code':'Get-Date'}).get('needs_confirmation')); \
print('Stop-Computer caught:', ce._danger_check('Stop-Computer') is not None)"
  -> both print True.


-----------------------------------------------------------------------
R3-1 / R3-2 - Workflows: no re-entrant dispatch (a running workflow
        blocks new commands; a cron fire is dropped while busy)
-----------------------------------------------------------------------
Background: workflow steps run on the UI thread and pump the event loop
between steps. Before this fix a command typed mid-workflow - or a
scheduled (cron) workflow firing while another was running / a card was
up - could re-enter dispatch and overlap or corrupt the running workflow,
or replace a confirmation card out from under you. Now a workflow marks
itself "in flight" and both entry points respect it: _process_cmd rejects
new commands, and _on_scheduled_workflow_fire drops the cron fire.

NOTE: keep AUTO_CONFIRM OFF so the confirm step in TEST B actually pauses.
Turn DEBUG on to see the "skipped - JARVIS busy" scheduler log in TEST C.

TEST A - a new command is rejected while a workflow runs:
  1. Run a multi-step workflow that takes a few seconds, e.g. say / type:
        "open chrome and search youtube for lofi and take a screenshot"
     (the brain calls between steps give you a 1-2 s window).
  2. While it's mid-run (HUD shows "Automation: step 2/3" etc.), quickly
     type another command in the command bar, e.g. "what time is it".
  PASS (fixed): a toast "A workflow is running - please wait." appears and
                the second command does NOT run. The workflow finishes its
                own steps cleanly, in order.
  FAIL (old):   the typed command dispatched in the middle of the workflow
                (interleaved steps, doubled output, or a corrupted run).

TEST B - a workflow that pauses on a confirm still resumes (regression
         guard for the in-flight COUNTER - the important one):
  1. Run a workflow whose steps include a file creation, e.g.:
        "create a file called wf_test.txt then open chrome"
  2. It pauses on the create-file confirmation card.
  3. Reply "yes" (typed or spoken).
  PASS (fixed): the file is created AND the workflow resumes and finishes
                the remaining steps (chrome opens). The "yes" is NOT blocked
                by the "workflow is running" toast - it resolves the card,
                and the resume frame is still counted as in-flight.
  FAIL (broken): the "yes" is rejected as a new command, or the workflow
                never resumes after the confirm (a plain bool flag instead
                of a counter would clear early here and break the resume).
  Cleanup: delete wf_test.txt afterwards.

TEST C - a scheduled (cron) workflow is dropped while JARVIS is busy:
  1. Create a workflow on a 1-minute schedule, e.g.:
        "create a workflow called ping that takes a screenshot, every minute"
     (cron "* * * * *").
  2. Keep JARVIS busy as the minute ticks over - start a second, slower
     manual workflow, or trigger any confirmation card and leave it open.
  PASS (fixed): the scheduled "ping" does NOT fire on top of the busy state.
                With DEBUG on you'll see a log line like:
                  [scheduler] scheduled workflow 'ping' skipped - JARVIS busy (state=...)
                The next minute, when idle, it fires normally.
  FAIL (old):   "ping" fired mid-confirmation / mid-workflow, overlapping
                dispatch and possibly replacing the open card.
  Cleanup: "delete the workflow ping" when done.

Code-level proof (no UI, no cron) - from repo root; drives a real workflow
with a fake dispatch and asserts the in-flight flag is True during each
step and False after:
  uv run python -c "import core.executor as ex, core.handlers.automation_handler as ah; \
seen=[]; ex.dispatch=lambda step,**k: (seen.append(ah.is_workflow_in_flight()), {'success':True,'response':'ok','intent':step.get('intent','x')})[1]; \
wf={'task_name':'t','steps':[{'intent':'open_app','action':'open_browser','parameters':{}},{'intent':'system_control','action':'screenshot','parameters':{}}]}; \
out=ah._handle_automation_task('run_workflow',wf); \
print('in-flight during steps:', seen, '(want [True, True])'); \
print('in-flight after return :', ah.is_workflow_in_flight(), '(want False)')"
  -> expect:  in-flight during steps: [True, True] ... after return: False


-----------------------------------------------------------------------
R3-8 / R3-9 - Scheduler: no double-fire on restart, and catch-up for a slot
        missed while JARVIS was closed (or the machine was asleep)
-----------------------------------------------------------------------
Background: the scheduler now persists the last *scheduled instant* it fired
per workflow to data/scheduler_state.json. That kills the restart double-fire
(R3-8) and lets a tick after downtime catch up a missed slot (R3-9). From the
scheduler's point of view "machine asleep" and "JARVIS closed" are the same
thing - no tick runs - so closing the app is a faithful way to test both.

Setup (used by both tests):
  Create a per-minute workflow that leaves an obvious trace each time it fires:
     "create a workflow called ping that takes a screenshot and saves it to
      C:\Users\Lenovo\Documents\jarvis-project\tests, every minute"
  Make sure it's enabled (ENABLED ON in the AUTOMATE page, or say "enable ping").
  Each fire = one new JARVIS_<date>_<time>.png in tests\ AND a console line:
     [scheduler] firing workflow 'ping' (schedule='* * * * *')
  Keep the PS console visible. Clean up the screenshots when done.

TEST A - R3-8: no double-fire when you restart within the same minute
  1. Watch the console; wait for ONE "firing workflow 'ping'" line (note the
     minute, e.g. 02:41, and that one screenshot appeared).
  2. IMMEDIATELY - still within that same minute - close JARVIS and restart it
     (uv run python main.py).
  PASS (fixed): during the rest of that same minute, ping does NOT fire again -
                no second screenshot for that minute, no second "firing" line.
                On the next minute boundary it fires once, normally.
  FAIL (old):   ping fired a second time for the same minute right after restart
                (the in-memory guard was empty on a fresh start).
  Proof on disk: open data\scheduler_state.json - it has a "ping" entry with the
                 last-fired ISO instant; that's what survived the restart.

TEST B - R3-9: catch-up for a slot missed while JARVIS was closed/asleep
  1. Let ping fire at least once (so it has a recorded last-fired instant).
  2. Close JARVIS. Leave it closed for 2-3 minutes (this is the "asleep over the
     scheduled slot" condition - several per-minute slots pass with no tick).
  3. Reopen JARVIS (uv run python main.py) and watch the console.
  PASS (fixed): within a few seconds of startup you get exactly ONE "firing
                workflow 'ping'" line (one catch-up screenshot) for the most
                recent missed minute - NOT one per missed minute. Then it
                resumes firing once per minute.
  FAIL (old):   nothing fired to make up the missed slot (it was silently lost);
                "every day at 9" would simply not run if asleep at 9.
  Storm-safety note: even if you leave it closed for an hour, reopening fires
                ONCE (the latest missed slot), never a backlog flood.

  (A daily "30 9 * * *" behaves the same - it just takes a day to exercise by
   hand, which is why the per-minute proxy above is the practical test. For the
   exact instant logic without waiting, run the automated proof below.)

Code-level proof (no waiting, no restart) - from repo root:
  uv run pytest -q tests/test_scheduler_catchup.py
  -> 12 passed: restart-same-minute no double-fire, catch-up after downtime,
     per-minute advance, long-downtime collapses to one fire, persistence
     round-trip, and frozen-clock _tick integration.


-----------------------------------------------------------------------
R3-15 / R3-16 / R3-17 - Wake word: clean mic handoff, no recognition gap,
        and a visible warning when speech recognition goes offline
-----------------------------------------------------------------------
Background: the always-on wake detector now (R3-17) recognises on a separate
thread from capture, so the mic keeps listening during the network call;
(R3-16) classifies STT errors and warns once if recognition keeps failing
instead of dying silently; and (R3-15) hands the mic off to the main capture
with a real "stream closed" event instead of a fixed sleep.

These run on real mic + network, so most of the verification is "does the wake
word still work smoothly?" The exact logic is covered by the automated proof at
the bottom - run that first; the manual checks below are for the live feel.

Setup: turn DEBUG on (Settings or .env DEBUG=true) so the [wake] log lines show.
Keep the PS console visible.

TEST A - R3-17: wake word still triggers reliably, including back-to-back
  1. With JARVIS idle, say "Jarvis" - the orb should switch to listening.
  2. Let it go back to idle, then say "Jarvis" again a few times at a normal
     pace, including right after it returns to idle.
  PASS (fixed): each "Jarvis" is caught; you don't have to repeat it because it
                "wasn't listening yet". In the log you'll see [wake] heard: ...
                lines appearing continuously (capture never stalls for the STT
                call).
  FAIL (old):   some "Jarvis" said while it was mid-recognition were missed.
  Note: this is probabilistic - the old gap was intermittent, so "it just works
        every time now" is the signal. The automated test pins the mechanism.

TEST B - R3-15: no mic conflict on the handoff
  1. Rapidly trigger listening several times: say "Jarvis", let it idle, repeat
     quickly; or use the mic button repeatedly.
  PASS (fixed): listening opens cleanly each time; no bursts of "no speech" /
                device-busy errors in the log. You may see a one-off
                [voice] wake stream-closed wait timed out; proceeding - that's
                the safe fallback, not an error.
  FAIL (old):   intermittent capture failures / "no speech" right after a wake,
                from two streams briefly sharing the mic device.

TEST C - R3-16: offline warning is visible (optional, needs forcing a failure)
  Easiest way to force persistent STT failure: disconnect the network (turn off
  Wi-Fi) and then make noise near the mic so the detector runs STT on non-silent
  windows.
  PASS (fixed): after ~5 consecutive failed recognitions you get ONE toast:
                "Wake word offline - speech service unavailable." (and a [wake]
                warning line). It does NOT spam - just once. Reconnect Wi-Fi and
                the next successful recognition silently re-arms the warning.
  FAIL (old):   wake word just stops working with nothing in the log or UI.
  (Skip if you don't want to toggle Wi-Fi - the automated test covers the
   threshold + warn-once + reset logic exactly.)

Code-level proof (no mic, no network) - from repo root:
  uv run pytest -q tests/test_wake_word.py
  -> 11 passed: STT error classification (benign vs counted), warn-once at the
     threshold + reset on success, recognise/wake-match/paused-skip paths, and
     the stream-closed handshake event semantics.


-----------------------------------------------------------------------
R3-6 - Confirmation system: a second confirmation can't clobber the first,
        and multi-confirm workflows still resume correctly
-----------------------------------------------------------------------
Background: there's one pending-confirmation slot. Before, a second
request_confirmation overwrote it and silently dropped the first callback -
including a paused workflow's "resume the rest of my steps" continuation. Now a
second request while one is outstanding is REFUSED (the first is preserved).

Note: R3-1/R3-2 already stop you from triggering a second confirmation through
normal use (typing a new command while a card is up gives the "respond to the
pending confirmation first" toast). So R3-6 is mostly a backstop at the
primitive level - the observable, important thing to verify is that the WORKFLOW
resume it protects still works.

Keep AUTO_CONFIRM OFF so the cards appear.

TEST A - multi-confirm workflow resumes through BOTH confirmations:
  1. Run a workflow with two file-creation steps, e.g. say / type:
        "create a file called r6a.txt then create a file called r6b.txt"
  2. It pauses on the first card (r6a.txt). Reply "yes".
  3. It must then pause on the SECOND card (r6b.txt). Reply "yes".
  PASS (fixed): both files are created and the terminal shows "all 2 steps
                complete". The second confirmation registering did NOT get
                refused (the slot was already cleared when step 2 re-registered),
                and the first step's continuation was never lost.
  FAIL (broken): only r6a.txt is created and the workflow never asks about /
                creates r6b.txt (lost continuation), or the second card is
                wrongly refused.
  Cleanup: delete r6a.txt and r6b.txt.

TEST B - an ambiguous reply on an open card is rejected, card survives:
  1. Trigger a confirmation, e.g. "delete the file r6a.txt" (a card appears).
  2. Without answering, type something that's neither yes/no, e.g. "open chrome".
  PASS (fixed): you get "Please answer the confirmation first — yes or no.",
                Chrome does NOT open, the delete does NOT happen, and the card is
                still there to answer. (Short inputs like "open chrome" used to be
                read as a non-yes reply and silently stand the card down.)
  3. Now answer the card properly: "yes" deletes r6a.txt; "no"/"cancel" stands
     down. A clear yes/no always resolves the card.
  Note: a *long, clearly-new* directive (e.g. "create a file called x with
  content y") instead ABANDONS the stale card and runs as a new command — that's
  intentional (you changed your mind); only short/ambiguous input keeps the card.
  FAIL (old):   "open chrome" silently cancelled the delete (stood down) without
                opening Chrome, or the second command ran.

Code-level proof (no UI) - from repo root:
  uv run pytest -q tests/test_confirmation_lock.py
  -> refuse-preserves-first (second callback never runs, first does),
     request-succeeds-after-resolve (the resume case), succeeds-after-abandon,
     decisive vs ambiguous reply classification (yes/no resolve; "open chrome",
     bare "please" keep the card),
     no-leak-between-resolutions, threaded no-double-fire, resolve-with-nothing.


-----------------------------------------------------------------------
R3-13 - Privacy: dictated secrets no longer persist to memory in plaintext
-----------------------------------------------------------------------
Background: conversation memory (data/memory.jsonl) stored the model's JSON
verbatim, including the content/text/value/code parameters - i.e. dictated file
bodies, passwords, API keys, code. Those were written in plaintext and reloaded
into the prompt every session. Now those long param values are redacted to
"<N chars>" before memory is stored/persisted. The user's free-form command
text is intentionally left as-is (redacting arbitrary prose reliably isn't
feasible without mangling normal commands).

Do this:
  1. Issue a command that puts a secret into a structured parameter, e.g.:
        "create a file called pw.txt with the content my password is hunter2hunter2"
     (make the content > 24 chars so it's treated as a real payload). Confirm
     the file if asked.
  2. Open data/memory.jsonl in an editor (it's in the project's data/ folder).
     Look at the most recent assistant line.
  PASS (fixed): the assistant entry shows the content as "<NN chars>" (redacted),
                NOT your literal "my password is hunter2hunter2". The file you
                created on disk still has the real content - only the conversation
                MEMORY is redacted.
  FAIL (old):   the literal secret text appears verbatim in memory.jsonl.

  Legacy scrub: if memory.jsonl already had a plaintext secret from before this
  fix, it's redacted on load and rewritten redacted on the next exchange. To
  hard-reset everything, say "wipe your memory".

  Note: cmd_text (your spoken/typed command itself) is NOT redacted by design -
  only the structured parameter payloads are.

Code-level proof - from repo root:
  uv run pytest -q tests/test_memory_redaction.py
  -> 6 passed: long secret redacted, short value preserved, persisted file
     redacted, non-JSON stored unchanged, non-secret params kept, legacy
     plaintext scrubbed on load.


-----------------------------------------------------------------------
R3-20 - Timeouts kill the whole child process tree (no orphans)
-----------------------------------------------------------------------
Background: a streamed shell command that times out used proc.kill(), which
reaps only the DIRECT child. A command that spawned its own children (a shell
launching a server, npm spawning node) left grandchildren running after the
timeout. Now the command is launched in its own process group/session and a
timeout kills the entire tree (Windows: taskkill /F /T; POSIX: killpg).

This needs a grandchild-spawning command + a timeout, so it's mostly proven by
the automated test below. Optional manual check (Windows):
  1. Open Task Manager (Details tab), sort by name.
  2. Via JARVIS, run a streamed command that launches a child and then hangs
     past the 30 s timeout - e.g. a python one-liner that starts a child python
     sleeping and then sleeps itself. (Easiest through @code run_shell with a
     command that backgrounds a child.)
  3. Wait for the "Command timed out after 30s" line.
  PASS (fixed): after the timeout, NO leftover python.exe children from that
                command remain in Task Manager.
  FAIL (old):   a child/grandchild python.exe keeps running after the timeout.

Code-level proof (spawns a real parent+grandchild, no waiting on a 30 s timeout)
- from repo root:
  uv run pytest -q tests/test_kill_process_tree.py
  -> 1 passed: a real parent process that spawns a real grandchild is fully
     reaped by _kill_process_tree (both parent and grandchild gone).


-----------------------------------------------------------------------
BROWSER - persistent Chrome profile (fewer Google "unusual traffic" CAPTCHAs)
-----------------------------------------------------------------------
Background: the controlled browser used a fresh, cookie-less context every JARVIS
launch — to Google that looked like an anonymous automated browser, so searches
often hit the "I'm not a robot" reCAPTCHA. Now it launches a PERSISTENT profile
(data/browser_profile, gitignored) and hides the automation flag, so cookies/
history accumulate and it looks like a returning human.

Do this:
  1. Start JARVIS, do a few web searches over a couple of sessions (restart
     JARVIS in between).
  PASS (improved): the reCAPTCHA appears far less often than before; the browser
                remembers sites between runs (you stay logged into things you
                signed into in JARVIS's browser); and BOTH banners are GONE — the
                "Chrome is being controlled by automated test software" one
                (--enable-automation dropped) AND the "unsupported flag
                --no-sandbox, security will suffer" one (--no-sandbox dropped,
                which also re-enables Chrome's sandbox = more secure).
  Big win: in JARVIS's browser window, sign in to your Google account ONCE.
           Logged-in Google sessions get challenged dramatically less.

IMPORTANT - this is mitigation, not a cure:
  The remaining CAPTCHAs are mostly your NETWORK, not the code. A mobile-hotspot
  (carrier CGNAT) or VPN IP is shared by many users and Google rate-limits it
  hard — it'll still challenge sometimes regardless of the browser profile. A
  wired / home Wi-Fi connection reduces it the most. (Same IP reputation is why
  ElevenLabs/Gemini also flagged "unusual activity" on the hotspot.)

  If the browser ever fails to start with "profile is in use", close any other
  JARVIS instance, or delete the data/browser_profile folder and relaunch.

Code-level proof (no real Chrome) - from repo root:
  uv run pytest -q tests/test_browser_persistent_profile.py
  -> 3 passed: launch_persistent_context used (not launch()+new_context()) with
     the data/browser_profile dir + --disable-blink-features=AutomationControlled;
     reuses the persistent context's initial page; friendly "profile in use" error.


-----------------------------------------------------------------------
BROWSER - "screenshot of the browser" captures the PAGE, not the active window
-----------------------------------------------------------------------
Background: "take a screenshot of the browser" used to route to
system_control/screenshot — an OS grab of whatever window was on top. It only
looked right when Chrome was the active window; with JARVIS focused it captured
JARVIS instead. Now that phrasing routes to browser_automation/screenshot, which
captures the controlled browser PAGE via Playwright (full-page, even below the
fold) regardless of focus, and still saves to a folder.
(Routing-only change in CLAUDE.md — verified live, not unit-testable.)

Do this:
  1. Have JARVIS open a page: "open chrome and navigate to youtube".
  2. Click on the JARVIS window so IT is the active/focused window (NOT Chrome).
  3. Say/type: "take a screenshot of the browser and save it to jarvis-project/tests/"
  PASS (fixed): the saved PNG shows the YOUTUBE PAGE (the browser content), even
                though JARVIS was the focused window. Debug shows
                INTENT: browser_automation / ACTION: screenshot (not
                system_control). It also captures full-page (content below the
                visible viewport).
  Naming: brain-driven, like a document topic, and saved EXACTLY as named (NO
                timestamp). JARVIS picks a meaningful filename from context (e.g.
                "screenshot the whatsapp web page" → ".../whatsapp_web.png") and
                that's the exact file on disk. Because there's no hidden timestamp,
                JARVIS knows the name and can later "delete that screenshot" /
                "open it" using the same filename. Re-using a name OVERWRITES the
                previous shot (say a different name to keep both). Uses the SAME
                slugify as docs. Fallback when the brain gives only a folder: page
                title (browser) or "screen" (OS). Applies to BOTH screenshot types.
                Try: "screenshot the youtube page to tests/" → a file like
                "youtube.png" (a name relating to the content), then "delete that
                screenshot" → JARVIS deletes the same youtube.png.
  FAIL (old):   the PNG showed the JARVIS window (system_control screen grab of
                the active window).

  Still works: a plain "take a screenshot" (no browser mentioned) → OS screen
  grab via system_control/screenshot, as before. Only "...of the browser/page/
  site/tab" routes to the browser capture.


-----------------------------------------------------------------------
SCREEN READING - humanized spoken description, not a raw OCR / markdown dump
-----------------------------------------------------------------------
Background: "read my screen" used to route to read_screen (raw Tesseract OCR),
so JARVIS spoke a garbled element-by-element dump. And "what's on my screen"
(vision describe) returned a long structured markdown report — also bad for TTS.
Now both route to vision_analysis/describe with a brief, conversational,
spoken-style prompt (a few sentences, no markdown headings/symbols, may use a
short bullet list only when it genuinely helps; never transcribes everything).

Do this:
  1. Say/type: "read my screen"  (and separately: "what's on my screen").
  PASS (fixed): JARVIS gives a short NATURAL description — leads with the gist
                (which app/page), then a few notable things — the way it
                describes an image. Debug shows INTENT: vision_analysis /
                ACTION: describe (NOT read_screen / ocr_active_window). No
                garbled OCR, no "## headings" or "**bold**" read aloud.
  FAIL (old):   it routed to read_screen and spoke raw OCR ("my.ini GEMINLA o
                .env php.ini ..."), or dumped a long markdown report.

  Verbatim still available: "read the EXACT text on screen" / "what does it say
  word-for-word" → read_screen OCR (or vision read_text) returns the literal
  text.

  Find an element: "find the Upgrade button on my screen" → vision_analysis/
  find_ui_element. PASS: a short SPOKEN answer like "It's top-right near the
  profile, around (1288, 63)" — NOT a markdown report with ## headings/bullets.

Code-level proof (no model/screen) - from repo root:
  uv run pytest -q tests/test_vision.py
  -> describe prompt is the brief spoken-style one (JARVIS, "out loud", "don't
     transcribe/dump everything", bullets-only-when-helpful); read_text stays
     verbatim; find_ui_element / answer_question unchanged.


-----------------------------------------------------------------------
TERMINAL - copy a command block's text to the clipboard (no more screenshots)
-----------------------------------------------------------------------
Background: terminal output was rendered as QLabels you couldn't select, so to
share output you had to screenshot it. Now each command block has a small copy
button, and the text is selectable.

Do this:
  1. Run anything in the Terminal page (e.g. "read my screen", "git status").
  2. Hover the command block — a small "⧉" button sits at the right of the
     prompt row. Click it (it briefly flashes "✓").
  3. Paste anywhere (Ctrl+V).
  PASS (fixed): the clipboard has the WHOLE block as plain text — the prompt,
                JARVIS's reply, and every output line, newline-separated. No
                screenshot needed.
  Also: you can now select text in a line/label with the mouse and Ctrl+C it
        (single labels; the ⧉ button is the reliable way to grab a whole block,
        since selection can't span multiple lines).

Code-level proof - from repo root:
  uv run pytest -q tests/test_terminal_copy.py
  -> 4 passed: _block_text joins prompt + reply + all output lines (and handles
     empty prompt/reply/block).


-----------------------------------------------------------------------
BROWSER - full page vs visible-area screenshot
-----------------------------------------------------------------------
Background: browser screenshots were hardcoded to full-page (entire scrollable
page). Now you can choose: full_page defaults TRUE (whole page), and you opt into
the visible viewport only.

Do this (with a long page open, e.g. a YouTube feed):
  1. "screenshot the browser" or "screenshot the full page" → full_page=true.
     PASS: the PNG is the ENTIRE scrollable page (tall image, content below the
     fold included). [default — nothing changed here.]
  2. "screenshot just what's visible" / "the visible part" / "the current view"
     / "what's on screen now" / "without scrolling" → full_page=false.
     PASS: the PNG is ONLY the visible viewport (one screen height), not the
     whole page. Debug shows the brain set "full_page": false.

  (You don't need the word "viewport" — the phrases above all map to viewport-
  only. A plain "screenshot the browser" stays full-page.)

Code-level proof - from repo root:
  uv run pytest -q tests/test_browser_screenshot_fullpage.py
  -> screenshot_page defaults full_page=True, honors full_page=False; the
     handler's _as_bool coerces true/false/yes/no/0/1 correctly.


-----------------------------------------------------------------------
BROWSER - capture a SECTION of the page by natural language (B1)
-----------------------------------------------------------------------
Background: you can now screenshot just a part of the page by naming it — JARVIS
uses the same snapshot picker as click/fill to find that element and capture only
its box (not the whole page).

Do this (on a page with clear sections, e.g. YouTube or a ChatGPT chat):
  1. "screenshot the comments section" / "screenshot the video player" /
     "screenshot the sidebar" / "capture just the search results"
  PASS (fixed): the saved PNG is ONLY that section, not the whole page. Debug
                shows browser_automation/screenshot with a "goal" param, and the
                terminal shows "❯ screenshot area '<goal>'" then "↳ picked
                ref_N (<role> "<name>")" → "✓ area captured".
  If not found: you get a clean "Couldn't find '<goal>' on the page to
                screenshot — try the full page, or name the section differently."
                (not a crash). Then fall back to a full-page or visible-area shot.

  Routing reminder: name a SECTION → goal (this). "the page/browser/site" →
  full page. "just what's visible" → viewport. (See the two entries above.)

Code-level proof - from repo root:
  uv run pytest -q tests/test_browser_screenshot_area.py
  -> goal routes to find_and_act(..., "screenshot"); plain screenshot → whole/
     visible page; an explicit CSS selector still wins over goal.


BROWSER - capture a MULTI-ELEMENT region (heading + input + items below)
-----------------------------------------------------------------------
Background: a "section" often isn't one DOM node — it's several (a heading, an
input, and the items under it). The screenshot picker now asks Haiku for the
*set* of refs that bound the region, unions their document-space boxes, and clips
the page with page.screenshot(clip=union, full_page=True) so even below-the-fold
spans are captured. A single element still gets a crisp element-level shot.

Do this (e.g. on the ChatGPT new-chat page):
  1. "screenshot the 'What's on the agenda today?' heading, the input field, and
     the suggestions below it, into tests/agenda.png"
  PASS (fixed): tests/agenda.png contains the heading AND the input box AND the
                three suggestion rows — all in one image (previously only the
                heading was captured). Terminal shows "❯ screenshot area '<goal>'"
                then "↳ picked N elements: ref_a, ref_b, ..." → "✓ saved →
                agenda.png".
  2. Single-part still tight: "screenshot just the input box" -> terminal shows
     "↳ picked ref_N (...)" → "✓ area captured" (element-level shot, not a clip).
  3. Below the fold: scroll up, then "screenshot the footer links section" — the
     clip still lands on the footer (document coords + full_page handle scroll).

  Routing reminder: list several adjacent parts in ONE goal string ("X and Y and
  the Z below") — do NOT split into multiple screenshots or a workflow. The whole
  span goes in one goal; the picker bounds them together.

Code-level proof - from repo root:
  uv run pytest -q tests/test_browser_screenshot_region.py
  -> _parse_haiku_refs handles {"refs":[...]} + degrades {"ref":N}; _union_clip
     spans all boxes with padding; one ref → element shot; many → clip+full_page;
     no refs → legacy fallback; unresolvable boxes → clean error, no shot.


BROWSER - multi-engine foundation (Edge/Firefox) — PHASE 1 (no UI change yet)
-----------------------------------------------------------------------
Background: groundwork for controlling Edge and Firefox (not just Chrome). The
session now runs engines CONCURRENTLY off one Playwright driver — Chrome, Edge,
Firefox can all be alive at once; the "active" engine is a pointer, so switching
closes nothing. Phase 1 is foundation ONLY: behaviour is byte-identical to before
(Chrome is still the one and only engine that opens). "open edge" does NOT yet
switch — that's Phase 2.

Do this (sanity — prove nothing regressed):
  1. "open chrome" / "go to youtube" / "screenshot the browser" / open+search in
     one line -> all behave EXACTLY as before. Chrome uses its same warmed
     profile (data/browser_profile) — still logged into Google, no extra CAPTCHAs.
  PASS: no behavioural difference vs. last session.

  Note: two new profile dirs (data/browser_profile_edge, _firefox) are created
  lazily only when those engines are first launched (Phase 2/3). They're
  gitignored. Chrome's profile path is unchanged.

Code-level proof - from repo root:
  uv run pytest -q tests/test_browser_engines.py tests/test_browser_persistent_profile.py
  -> _launch_config gives chrome(channel=chrome)/edge(channel=msedge, same flags)/
     firefox(type=firefox, prefs, no chromium switches); per-engine profile dirs;
     _resolve_engine (explicit/default/auto); _engine_available detection;
     _page/_context/_ready/_ref_map route to the active engine; engines launch
     concurrently and switching back does NOT relaunch; stop() closes all;
     _recover() relaunches only the active engine. Chrome launch kwargs unchanged.


BROWSER - control Edge / switch engines — PHASE 2 (live, no confirm)
-----------------------------------------------------------------------
Background: "open edge" / "switch to firefox" now actually changes which browser
JARVIS controls. Engines run concurrently — switching is an instant pointer flip
that closes NOTHING (no confirm). Bare commands after a switch hit the now-active
engine. (Firefox needs its one-time build — see Phase 3; until then "open
firefox" may report it isn't installed, which is the correct clean error.)

Do this:
  1. "open chrome" -> Chrome controlled as before. Navigate somewhere.
  2. "open edge"   -> Debug shows open_app/open_browser browser="edge". Terminal:
                      "❯ edge browser" → "✓ edge ready". A controlled Edge window
                      opens (fresh Edge profile, not your logged-in one). Chrome
                      STAYS open in the background.
  PASS: Edge is now the active browser.
  3. "go to youtube" / "screenshot the browser" -> these hit EDGE now.
  4. "switch to chrome" -> instant, no new window, Chrome is active again, your
                      Chrome tabs are exactly as you left them (nothing closed).
  PASS: switching back is instant and lossless.
  5. "open edge" again while Edge already active -> "Already on edge", no-op.

  Engine-vs-tab routing check (must NOT confuse the two):
    - "switch to edge"        -> open_browser (engine)   [browser app]
    - "switch to the youtube tab" -> switch_tab (tab)    [open site/tab]

  Not installed: "open firefox" with no FF build -> spoken "Firefox engine isn't
  installed — run: playwright install firefox" (clean, no crash). Same shape for
  Edge on a machine without Edge.

Code-level proof - from repo root:
  uv run pytest -q tests/test_browser_engine_switch.py
  -> ensure_engine: cold launch (edge→channel msedge); switch = pointer flip, no
     relaunch, nothing closed; already-active no-op; not-installed clean error.
     open_browser handler maps spoken name → engine and drives ensure_engine.


BROWSER - Firefox engine + close one engine — PHASE 3 (live)
-----------------------------------------------------------------------
Background: Firefox is now a third controllable engine, and you can close ONE
engine while the others stay alive. Firefox uses Playwright's OWN bundled build
(NOT your system Mozilla) — a one-time `uv run playwright install firefox` was
run; if a fresh machine lacks it, "open firefox" says so cleanly.

Do this:
  1. "open firefox" -> a controlled Firefox window opens (its own profile). Every
     feature works (it reuses the same mixins as Chrome/Edge):
       - "go to example.com" / "read the page"
       - "screenshot the heading and the paragraph below it" (union-clip region
         capture works on Firefox too — verified)
  PASS: Firefox is driven exactly like Chrome/Edge.
  2. With Chrome + Edge + Firefox all opened, "switch to chrome" etc. flips
     instantly, nothing closes.
  3. "close edge" -> closes ONLY Edge; Chrome + Firefox stay open. Debug shows
     browser_automation/close_engine browser="edge". Terminal "✓ Closed edge —
     now on <remaining>". If Edge was active, active falls back to a remaining
     engine.
  4. "close the browser" (no name) -> closes the ACTIVE engine.
  5. Close the last open engine -> the Playwright driver is released; the next
     "open chrome" starts fresh.

  Routing checks (three distinct things — must not be confused):
    - "close edge"            -> close_engine (whole browser engine)
    - "close the youtube tab" -> close_tab    (one tab inside the browser)
    - close_app/force_quit    -> only for unrelated desktop apps, NEVER a
                                 controlled browser engine.

  Not installed (fresh machine): "open firefox" -> "Firefox engine isn't
  installed — run: playwright install firefox" (clean, no crash).

Code-level proof - from repo root:
  uv run pytest -q tests/test_browser_engine_switch.py tests/test_browser_engines.py
  -> close_engine closes one + keeps others; re-points active; closing last
     engine stops the driver; not-open / nothing-open clean errors; close route
     never auto-starts a browser. Firefox launch-config (type=firefox, prefs, no
     chromium switches) + detection.

LIVE parity runner (opens real Edge + Firefox windows, hits Haiku + network):
  uv run python tests/_live_engine_parity.py
  -> drives EVERY browser capability on Edge then Firefox: read/extract, click
     (selector/text/Haiku goal), fill (fields/Haiku goal), screenshots (full/
     viewport/element/region-union), scroll, tabs (new/list/switch/close),
     navigate + history (back/forward/refresh/hard_refresh). Last run:
     edge 25/25, firefox 25/25 — full Chrome parity confirmed on both engines.


BROWSER - "list my tabs" is now SPOKEN, not just shown
-----------------------------------------------------------------------
Background: list_tabs used to speak a generic ack ("Pulling up every open tab")
while the actual tab names only appeared in the terminal — you had to ask "what
are the names?" to hear them. list_tabs is now in _OUTPUT_IS_RESPONSE, and
render._clean_tabs_for_speech turns the terminal listing into a natural spoken
sentence.

Do this:
  1. Open a couple of tabs (e.g. youtube + github), then "list my tabs" /
     "what tabs do I have open".
  PASS (fixed): JARVIS SPEAKS the list — e.g. "2 tabs open: YouTube; GitHub,
                active." (titles, with the active one called out). The terminal
                still shows the full numbered "1. host — title *" listing.
  2. Blank/untitled tab -> spoken as "a blank tab"; an untitled non-blank tab
     falls back to its host.

  Scope note: only list_tabs changed. Other read commands already spoke their
  output (read_page, status_report, list_voices, list_reminders, list_workflows,
  file reads, weather, OCR, vision) — untouched.

Code-level proof - from repo root:
  uv run pytest -q tests/test_list_tabs_speech.py
  -> list_tabs is in _OUTPUT_IS_RESPONSE; _clean_tabs_for_speech formats the
     listing (active marker, blank-tab + host fallbacks); the assembler speaks
     the tab list as primary and drops the generic brain ack.


BROWSER - search instead of guessing unknown domains (brain prompt)
-----------------------------------------------------------------------
Background: the brain used to fabricate domains for informally-named sites
(e.g. "the mobbin website" -> https://www.mobbins.com), which DNS-fails. CLAUDE.md
now tells it: if NOT highly confident of a site's exact domain, route to
search_web/google_search with the name instead of guessing www.<name>.com.
(Prompt-only change — not unit-testable; verify by behaviour.)

Do this (restart JARVIS first so the brain reloads CLAUDE.md):
  1. "go to the mobbin website" (lesser-known name) -> Debug should show
     search_web/google_search query="mobbin" (NOT navigate to a guessed
     mobbins.com). You land on Google results with the real site on top.
  2. "go to mobbin.com" (explicit domain) -> navigate to https://mobbin.com
     (literal URL honoured — no search).
  3. "open youtube" / "go to github" (well-known) -> navigate directly to the
     known domain, NO search.
  PASS: uncertain names search; explicit URLs + well-known sites navigate.

  Note: applies to open_app/open_url too. Borderline calls lean toward search
  (a no-op search page beats a dead DNS guess).


REFACTOR - file_ops.py monolith → package (R2-17b, no behavior change)
-----------------------------------------------------------------------
Background: core/handlers/file_ops.py (1220 LOC) was split into a package:
  file_ops/__init__.py   — public entry _handle_file_operation + _DISPATCH table
  file_ops/_common.py    — constants, helpers, terminal-signal funnel
  create / read_write / replace / move_rename / delete / batch / search / info .py
Pure structural move — every action behaves exactly as before. Import path is
unchanged (core.executor still does `from core.handlers.file_ops import
_handle_file_operation`).

Do this (sanity — all file ops still work via voice/text):
  1. "create a file Documents/test/hello.txt with content hi" -> confirm card ->
     confirm -> file written. (exercises the create_file confirm closure that
     moved into create._op_create_file)
  2. "read hello.txt", "how big is hello.txt" (file_info), "list Documents/test",
     "find hi in Documents/test" (find_in_files), "rename hello.txt to hey.txt",
     "append a line to hey.txt", "in hey.txt replace hi with yo" (confirm),
     "delete hey.txt" (confirm), "delete all *.tmp in Downloads" (batch, confirm).
  PASS: identical behaviour + same confirmation cards as before the split.

Code-level proof - from repo root:
  uv run pytest -q tests/test_file_ops_package.py
  -> public entry importable; _DISPATCH covers all 14 actions; _ok/_err stay
     shared singletons (re-exported); unknown action -> _err; each _op_* lives in
     its module; create_file & replace_in_file confirm closures still write after
     "yes" (proves the nested _do_* closures survived extraction into _op_*).
  Plus the existing file-op coverage still passes unchanged:
  uv run pytest -q tests/test_executor.py tests/test_review_fixes.py tests/test_audit_promoted.py


-----------------------------------------------------------------------
R2-17a  main.py decomposition into the ui/main_window package
-----------------------------------------------------------------------
Background: the 1844-LOC root main.py was split. JarvisWindow stays ONE
QMainWindow/QObject — all 9 pyqtSignals and every connect() live on it in
__init__ (window.py). Method groups moved verbatim into plain mixin classes
(the core/browser pattern):
  ui/main_window/window.py            — JarvisWindow shell: signals + __init__ + _INTENT_HUD
  ui/main_window/app.py               — main() bootstrap
  ui/main_window/{voice,confirm,execution,backend_signals,settings,
                  state_hud,lifecycle}_mixin.py
  ui/main_window/constants.py         — shared module constants (no import cycle)
main.py is now a 20-line shim: `from ui.main_window import JarvisWindow, main`.
Pure structural move — zero behaviour change. Launch contract unchanged.

WHY a manual pass is required: pytest never instantiates JarvisWindow, so the
suite alone cannot prove Qt signal/slot wiring or that the four virtual
overrides (paintEvent/nativeEvent/closeEvent/resizeEvent) — now hosted on
mixins — are still dispatched by Qt. They ARE: proven empirically (see harness).

Do this (real launch + interactions):
  1. `uv run python main.py` -> window paints fully (gradient background =
     paintEvent firing from _StateHudMixin), maximises, no ImportError.
  2. Click through every sidebar page (Dashboard/Voice/Automation/History/
     Settings/Terminal) -> _nav works.
  3. Type "what time is it" -> spoken + transcript (full execution pipeline).
  4. Type "delete <some test file>" -> confirm card -> "yes" -> executes
     (proves the _resume_executor_confirm main-thread hop / _ConfirmMixin).
  5. Open YouTube then a "search for lofi and play the first result" style
     two-step workflow -> runs without "Cannot switch to a different thread"
     (proves dispatch()+Playwright stayed on the Qt main thread).
  6. Win+J from another app -> window summons (nativeEvent on _LifecycleMixin).
  7. Toggle Quick Settings (mic/tts/auto-confirm/dim) -> persists (_SettingsMixin).
  8. Close the window -> clean shutdown, no hung thread (closeEvent ordering:
     wake_detector.stop -> _stop_rtt_thread -> browser.stop -> history_store.close).
  PASS: every behaviour identical to pre-split.

Automated proof harness (instantiates the real window — NOT a pytest test):
  uv run python tests/smoke_window.py
  -> reports which class hosts each Qt virtual and that Qt fired it, e.g.:
       paintEvent host=_StateHudMixin    fired=N
       resizeEvent/closeEvent/nativeEvent host=_LifecycleMixin  fired=N
     and drives _nav, _set_state, a status toast, confirm card show/hide, and a
     real tell_time command through the full execution pipeline. RESULT: PASS.

Code-level proof - from repo root:
  uv run pytest -q tests/test_main_window_structure.py
  -> public surface re-exported from main + package; JarvisWindow MRO contains
     all 7 mixins; all 9 pyqtSignals stay class attrs of JarvisWindow (NOT on any
     mixin); cross-cutting constants reachable.
  uv run pytest -q -m qt tests/test_workflow_thread_hop.py
  -> the executor confirm thread-hop (_resume_executor_confirm QueuedConnection)
     still lands the callback on the main thread.

NOTE (mixin-hosted Qt virtuals): this worked — Qt dispatches virtual overrides
defined on a plain mixin as long as the final type is the QObject subclass.
Proven in Slice 2 before relying on it. The documented fallback (had paintEvent
NOT fired from the mixin) was to keep all four event handlers on JarvisWindow in
window.py; it was not needed.


SPEECH - file paths in spoken/displayed text keep their separator
-----------------------------------------------------------------------
Background: speech_compact() shortens long paths for TTS (C:\a\b\c\file.txt ->
"c/file.txt"). A double-compaction bug stripped the separator on Windows paths:
the drive-letter pass produced "tests/requirements.txt", then the unix-path pass
re-matched the "/requirements.txt" tail and dropped the slash -> "testsrequire-
ments.txt". Fixed with a (?<!\w) lookbehind so the unix-path regex only matches a
path that STARTS at a boundary, never a "/tail" glued to a preceding word.

Do this:
  1. Ask to delete a file that doesn't exist in a SUB-folder, e.g.
     "delete requirements.txt in the jarvis-project/tests/ folder" (when it's
     actually in root).
  PASS (fixed): the spoken/shown error reads "...tests/requirements.txt..."
                (separator intact), NOT "testsrequirements.txt".
  2. Any path JARVIS speaks (screenshot saved, file created) reads as
     "parent/name" with the slash present.

Code-level proof - from repo root:
  uv run pytest -q tests/test_speech_compact.py
  -> Windows path keeps its separator (no "testsrequirements.txt"); compacts to
     parent/name; genuine absolute unix paths still compact; a glued "/tail" is
     left alone; plain text untouched.


TTS - fail fast when a cloud provider is quota-locked (no minute-long stalls)
-----------------------------------------------------------------------
Background: when ElevenLabs (credits) or Gemini (daily) hit their quota, the
provider chain already disables that tier for the session after the first 429.
But that FIRST 429 used to cost several seconds of SDK retry/backoff — and the
locks reset every launch, so commands #1-2 of every fresh session stalled (you
saw ~1 min). Fixed: each provider call is now built to fail fast —
  Gemini:     genai.Client(http_options=one-attempt + 15s timeout)
  ElevenLabs: request_options={max_retries:0, timeout_in_seconds:15}
so a quota error surfaces in ~1s and the chain drops to the next tier (→ pyttsx3)
immediately. The tier order + session locks are unchanged.

Do this (only meaningful while a cloud provider is quota-capped):
  1. Fresh `uv run python main.py` with TTS ON and ElevenLabs/Gemini quota spent.
     Say "hello".
  PASS (fixed): the first reply still falls back to pyttsx3, but the stall is ~1-2s,
                not 30-60s. Terminal shows "...quota exceeded — disabling for this
                session..." then pyttsx3 — quickly.
  2. Subsequent commands are instant (session lock already skips dead tiers).
  Note: when quota is AVAILABLE, nothing changes — providers work as before. The
  15s timeout only guards true hangs; 429s return immediately regardless.

Code-level proof - from repo root:
  uv run pytest -q tests/test_tts_failfast.py
  -> Gemini client built with retry attempts==1 + timeout; degrades gracefully if
     the SDK lacks HttpRetryOptions; falls back to a plain client if http_options
     is rejected. ElevenLabs say()/convert() forward max_retries=0 + timeout.


-----------------------------------------------------------------------
TTS - ElevenLabs expressiveness upgrade (eleven_v3 + voice_settings)
-----------------------------------------------------------------------
Background: the ElevenLabs tier was pinned to eleven_multilingual_v2, which does
NOT perform the inline audio tags ([chuckles] / [sighs] / [whispers]) the brain
emits - they were ignored (or risked being read literally). Now the tier uses
eleven_v3 (the only model that performs tags) with tunable voice_settings, all
sourced from config/jarvis.json so you can retune without code changes:
    elevenlabs_model            "eleven_v3"
    elevenlabs_stability        0.0   (v3 quantises to 0.0 Creative / 0.5 Natural
                                       / 1.0 Robust - 0.0 = most expressive)
    elevenlabs_similarity_boost 0.75
    elevenlabs_style            0.6
    elevenlabs_use_speaker_boost true
If the key's tier can't reach v3, synthesis auto-falls-back to eleven_turbo_v2_5
then eleven_flash_v2_5 (both tag-blind, so audio tags are stripped before
synthesis on them) and caches the working model for the session. Quota/auth/
network errors still FAIL FAST and drop to Gemini -> pyttsx3 (tier chain
unchanged); only a model-access error walks the fallback. request_options stay
max_retries=0 + 15s timeout.

Do this (needs a funded ELEVENLABS_API_KEY in .env + TTS ON, not muted):
  1. Fresh `uv run python main.py`, say "hello" (or "tell me a joke").
  PASS: JARVIS speaks in the ElevenLabs voice; if the brain included a tag like
        [chuckles], you hear an actual chuckle/breath, not the word "chuckles".
  2. Edit config/jarvis.json -> "elevenlabs_stability": 0.5, relaunch, repeat.
  PASS: delivery is noticeably flatter/steadier (Natural mode) - proves the
        voice_settings are live and config-driven.

Live one-shot (REAL API call + audio, no app needed) - from repo root:
  uv run python tests/_live_elevenlabs_v3.py
  -> prints "model served = 'eleven_v3'" when v3 is on your tier (speaks a tagged
     line). If your tier lacks v3 it prints the fallback model it used instead.

Code-level proof - from repo root:
  uv run pytest -q tests/test_tts_elevenlabs_v3.py
  -> model_id + voice_settings forwarded on stream/convert/probe; tags kept on
     v3 and stripped on fallback models; model-unavailable walks v3->turbo->flash
     and caches the winner; quota error fails fast WITHOUT a fallback walk.


-----------------------------------------------------------------------
TTS - audio tags must survive the spoken-response pipeline (first_sentence)
-----------------------------------------------------------------------
Background: even with eleven_v3 + a tag-capable voice, the [laughs]/[sighs]/
[whispers] tags weren't rendering live - JARVIS spoke the words but made no
laugh/sigh. Root cause was NOT the API or the voice (a direct-to-API probe
laughed on all 5 voices). It was first_sentence() in core/responders/utils.py:
it truncates any spoken response over 150 chars at the first '.!?' to keep acks
short - and JARVIS puts the tag just before the punchline, i.e. in the tail that
got cut. So the tag was stripped before it ever reached ElevenLabs. Fix:
first_sentence() now returns the full line untouched when it contains an inline
[..] audio tag (tags are capped at one per response and these lines are short,
so brevity isn't a concern when expressiveness was intentional). Untagged long
responses still truncate exactly as before.

Do this (needs eleven_v3 + a funded key + TTS ON, not muted):
  1. Fresh `uv run python main.py`, say "tell me a funny joke about america".
  PASS: the response is spoken in FULL (setup + tag + punchline) and you hear an
        actual soft laugh where [laughs softly] sits - not just the words.
  OLD (broken): only the first sentence was spoken; the tag (and punchline) were
        silently dropped, so no laugh.
  2. With debug_mode on, watch the terminal / logs/terminal.log for the line:
        [tts] ElevenLabs -> API model='eleven_v3' text='...[laughs softly]...'
     The tag must be PRESENT in that text= string. (This is the ground-truth
     trace of exactly what reaches the ElevenLabs API.)

Audition by ear (REAL API, costs credits) - from repo root:
  uv run python tests/_v3_tag_probe.py voices       # same [laughs] across voices
  uv run python tests/_v3_tag_probe.py tags male-british   # one clip per tag
  -> writes tests/tag_audio/*.mp3 (gitignored). Listen; only ear-verified tags
     belong in CLAUDE.md's tag list.

Code-level proof - from repo root:
  uv run pytest -q tests/test_first_sentence_tags.py
  -> tagged >150-char response survives first_sentence intact (tag reaches TTS);
     untagged long response still truncates; assembler keeps the tag end-to-end.


-----------------------------------------------------------------------
Weather - spoken units are spelled out (no "41 C" / dropped degree symbol)
-----------------------------------------------------------------------
Background: the weather line is spoken verbatim by TTS (weather is an
output-is-response intent). It used to read "41C, feels like 40C, humidity 19%,
wind 9.6 m/s" - TTS said "forty-one C" and mangled %/m/s, and a "°" glyph is no
better (ElevenLabs v3 normalization isn't guaranteed; the pyttsx3 SAPI fallback
won't voice "°" as "degrees"). Fix: format_current_weather() now writes the
words - "41 degrees Celsius, feels like 40 degrees, humidity 19 percent, wind
9.6 meters per second" - deterministic on every TTS tier.

Also: the 2-letter country code is expanded to a full name (it used to read as
letters, "K W"). "KW" -> "Kuwait", "GH" -> "Ghana", "GB" -> "United Kingdom",
etc.; unmapped codes keep the raw code (no regression).

Do this:
  1. `uv run python main.py`, say "what's the weather in Kuwait City".
  PASS: spoken as "Weather in Kuwait City, Kuwait: ...41 degrees Celsius, feels
        like 40 degrees, humidity 19 percent, wind 9.6 meters per second". No
        "K W", no "41 C", no stray symbols.

Code-level proof - from repo root:
  uv run pytest -q tests/test_weather.py
  -> format spells out degrees Celsius / percent / meters per second, expands the
     country code to a full name, and contains no raw glyphs (°, 41C, 19%, m/s).


-----------------------------------------------------------------------
TTS - first clip of a session performs its audio tag (MCI cold-start)
-----------------------------------------------------------------------
Background: the FIRST spoken line of a session sometimes didn't perform its
audio tag (e.g. a joke's [laughs]), while every later line did. Cause was in
play_mp3_stream (core/tts_elevenlabs.py): it started Windows MCI playback on the
FIRST streamed chunk, and MCI fixes the clip length at `open` - so on a cold
first request (chunks dribble in slowly) it played only that opening sliver and
stopped, dropping the rest of the clip including a tag past the first words.
Warm requests arrived nearly complete by play-start, so they sounded fine - why
only the first line was affected. Fix: buffer the whole stream to the temp file
FIRST, then open + play the complete file, so MCI always sees the full duration.
Trade-off: playback now starts after synthesis finishes (negligible for short
assistant lines); the tier chain / fail-fast request_options are unchanged.

Do this:
  1. Fresh `uv run python main.py` (cold start), FIRST thing say "tell me a joke".
  PASS (fixed): the whole joke plays AND you hear the [laughs] - on the very
        first line, same as later lines.
  OLD (broken): first line cut short / no laugh; subsequent jokes were fine.
  2. Ask for a few more jokes - all still perform their tags.

Note: no unit test - this is Windows-MCI audio glue (needs a real audio device),
verified by ear like the other playback paths. The tag-reaches-API trace
([tts] ElevenLabs -> API text=...) still proves the text side is intact.


-----------------------------------------------------------------------
Streaming STT - Phase 1 foundation (interface + Deepgram session, NOT wired)
-----------------------------------------------------------------------
Background: replacing the Google batch STT (record-then-send, ~2.5s silence tail
+ HTTP round-trip) with real-time streaming. Phase 1 lays the non-breaking
foundation: a provider-agnostic StreamingSttSession interface (core/stt/base.py),
a Deepgram websocket implementation (core/stt/deepgram.py, nova-3), and a
config-driven factory (core/stt/factory.py). NOTHING is wired into the live
voice path yet - stt_provider defaults to "google", so JARVIS behaves exactly as
before. New config (config/settings.py + .env DEEPGRAM_API_KEY):
    stt_provider           "google" (default) | "deepgram"
    deepgram_model         "nova-3"
    stt_endpointing_ms     300
    stt_utterance_end_ms   1000
    stt_language           "en-US"

Manual test: none yet - there is no behaviour change until Phase 2 wires the
streaming loop into VoiceEngine.listen(). Phase 2 will add the live "say a
command, watch words stream in" test.

Code-level proof - from repo root:
  uv run pytest -q tests/test_stt_streaming.py
  -> factory returns None for google / missing key and a DeepgramSttSession for
     deepgram+key; the session maps interim->on_partial, is_final->on_final,
     UtteranceEnd->on_utterance_end, drops blank transcripts, and finish()
     flushes + closes the socket. (No network - the websocket is faked.)


-----------------------------------------------------------------------
Streaming STT - Phase 2 (wired into VoiceEngine.listen, Google batch fallback)
-----------------------------------------------------------------------
Background: Phase 2 makes streaming actually run. VoiceEngine.listen() now
branches: if create_stt_session(config) returns a session (stt_provider=
"deepgram" + a key), it drives the streaming path; otherwise it uses the
unchanged Google batch path. A new MicStreamer (core/stt/mic_streamer.py) pumps
raw 16 kHz PCM frames into the session - no local VAD, endpointing is Deepgram's
job. Server-side endpointing replaces the old 2.5s silence tail, so the turn
ends as soon as you stop talking (the latency win). Turn assembly accumulates
is_final segments; on UtteranceEnd it fires the callback (brain) exactly like
before, so main.py is untouched. Live interim words on the HUD come in Phase 3.

Safety / fallback: if streaming can't start (connect refused, mic init error) or
errors before producing ANY text, listen() automatically falls back to the
Google batch path for that turn. A genuine no-speech (connected, nothing said)
is reported as "No speech detected" - NOT retried as batch.

Manual test A - default unchanged (stt_provider still "google"):
  Leave config as-is. Say "what time is it" / "open chrome" -> works exactly as
  before (batch path). This proves Phase 2 is non-breaking by default.

Manual test B - streaming live (needs DEEPGRAM_API_KEY in .env):
  Set stt_provider = "deepgram" (config/jarvis.json or Settings), restart.
  Wake JARVIS, say a command. Expect:
    - the turn to END noticeably faster than batch (no ~2.5s tail), and
    - the [voice] log to show "using streaming STT" then "streaming final: ...".
  Pull the network mid-utterance once -> it should fall back to the Google batch
  path (log: "streaming ... falling back to Google batch") rather than hang.
  Stay silent through a listen window -> "No speech detected", no batch retry.

Code-level proof - from repo root:
  uv run pytest -q tests/test_voice_streaming.py
  -> _run_streaming_stt assembles finals into one utterance and calls the
     callback; uses the last partial when no final arrives; returns False (batch
     fallback) when the session can't start or errors before any text; reports a
     clean no-speech (not a fallback) on genuine silence; still delivers text
     when an error arrives AFTER a final. (No mic/network - session + MicStreamer
     are faked.)


-----------------------------------------------------------------------
Streaming STT - Phase 3 (live interim transcript in the command bar)
-----------------------------------------------------------------------
Background: Phase 3 is the HUD side. VoiceEngine already EMITS transcript_partial
on the streaming path (per is_final segment); nothing was rendering it. Now
window.py connects voice_engine.bridge.transcript_partial (first accessed on the
Qt main thread, then queued from the worker thread) to _on_transcript_partial,
which mirrors the interim words into the dashboard command bar as you speak -
dictation style. The FINAL commit is unchanged: the existing voice callback
(_on_voice_heard -> _process_cmd) still submits the turn, so there is no
double-submit. The command bar is cleared when the turn ends (heard or error).

Scope / safety: only the streaming path emits partials, so on the Google batch
path (stt_provider="google", the default) the command bar never changes - zero
behaviour change. Late partials arriving after the turn left "listening" are
ignored so they can't clobber a fresh state.

Manual test (needs stt_provider="deepgram" + DEEPGRAM_API_KEY):
  Wake JARVIS and speak a command slowly. Expect the words to APPEAR in the
  bottom command-bar input field (">_ ...") and grow/refine as you talk, then:
    - the field CLEARS and the command runs the instant you stop (final commit).
  Try the batch path (stt_provider="google"): the command bar stays on its
  "AWAITING DIRECTIVE..." placeholder while you speak - unchanged, as expected.

Code-level proof - from repo root:
  uv run pytest -q tests/test_voice_dictation.py
  -> _on_transcript_partial paints interim text into the command bar only while
     listening (ignored otherwise / when blank), each partial replaces the last,
     and the turn-end handlers (_on_voice_heard / _on_voice_error_ui) clear the
     dictation. (No Qt - the command-bar input is a fake.)


-----------------------------------------------------------------------
Streaming STT - audio reliability fix (decouple network send from mic thread)
-----------------------------------------------------------------------
Background: live testing showed intermittent turns where Deepgram fired
SpeechStarted (it heard speech ENERGY) but returned zero Results - i.e. the
audio was gappy/choppy, not silent (mic peak was a healthy 14-21% full-scale).
Root cause: MicStreamer's read loop did the websocket send INLINE
(stream.read -> feed -> send_media -> ws.send). When a send stalled on network
jitter, stream.read() wasn't called in time, PortAudio's input buffer
overran, and samples were dropped -> shredded audio Deepgram couldn't
transcribe. Classic "never do network I/O on the audio thread".

Fix: DeepgramSttSession.feed() now only ENQUEUES audio (non-blocking); a
dedicated writer thread drains the queue to send_media. The mic read loop never
blocks on the network. finish() flushes the queue (writer.join) BEFORE
send_finalize so the tail of the utterance still reaches Deepgram. Queue is
bounded (256 chunks ~ 16 s) with drop-oldest so a long stall can't grow
unbounded or block the mic.

Telemetry kept (one concise line per turn at debug): "mic streamer stopped:
N frames fed, peak=P (X% full-scale), overflows=K" and "Deepgram read loop
ended after N event(s)". overflows should be 0; "Deepgram send queue dropped
N frame(s)" only logs when the network genuinely lagged.

Manual test (stt_provider="deepgram"): say several commands in a row, waiting
for the mic to go live each time. Expect EVERY spoken turn to transcribe (no
"no speech within timeout" while you were clearly speaking), and the stop line
to show overflows=0. Before the fix, ~half of turns died with SpeechStarted
but no Results.

Code-level proof - from repo root:
  uv run pytest -q tests/test_stt_streaming.py
  -> feed() forwards audio through the queue+writer and finish() flushes it in
     order before close (test_feed_forwards_audio_then_stops_after_finish);
     event mapping + blank-drop unchanged. (Websocket is faked.)


-----------------------------------------------------------------------
Voice capture - readiness cue (CONNECTING -> LISTENING)
-----------------------------------------------------------------------
Background: the HUD flipped to LISTENING the instant a turn began, but capture
isn't actually live until the wake-handshake + Deepgram connect + mic open
finish (~1-1.5 s on the streaming path). Speaking in that window lost the first
words (Deepgram fired SpeechStarted but no Results). Fix: VoiceEngine now emits
a `capture_ready` signal the moment the mic + STT session are genuinely live
(streaming: right after MicStreamer.start(); batch: right before capture()).
The HUD holds on a new "connecting" state until then, only flipping to
"listening" on capture_ready. So the cue you wait for is real.

What "connecting" looks like (dashboard): amber CONNECTING pill, HUD status
CONNECTING, orb active but the mic icon does NOT pulse yet, and the command-bar
placeholder reads "CONNECTING…". On capture_ready it flips to the cyan
LISTENING pill, the mic pulses, and the placeholder reads "LISTENING…". The
VoiceView screen mirrors this (CONNECTING in amber -> LISTENING in green).

Routing of the held state: _toggle_mic / _on_wake_word / _on_tts_done set
"connecting"; _voice_capture guards on it; _on_capture_ready flips to
"listening". A partial/final that races ahead of capture_ready is tolerated
(still painted / still processed) so no command is ever dropped.

Manual test (stt_provider="deepgram"): trigger a listen (wake word / mic
button). Expect a brief amber CONNECTING, THEN cyan LISTENING + a pulsing mic.
Speak only after LISTENING -> the whole utterance is captured. Speaking during
CONNECTING is no longer the "lost first words" trap (and even if a word slips
through early it isn't dropped). On the Google batch path the CONNECTING phase
is near-instant (no websocket), which is expected.

Code-level proof - from repo root:
  uv run pytest -q tests/test_voice_dictation.py
  -> _on_capture_ready flips connecting->listening (and is a no-op otherwise);
     a partial paints during connecting; a final during connecting is still
     processed (never dropped).


-----------------------------------------------------------------------
Provider fallback notices (user-facing toasts, not just debug)
-----------------------------------------------------------------------
Background: when TTS drops from ElevenLabs to the Google (Gemini) voice, or down
to the offline (pyttsx3) voice, there's a noticeable pause while the backup
synthesises - but the only signal was a [tts] debug line. Same for STT dropping
from live Deepgram streaming to the Google batch path. Now those emit a short,
plain-English toast via the central signals hub (new signals.notice(message,
kind) -> window._on_notice -> dashboard toast). Deduped ONCE per target per
session so a degraded run never spams.

These notices precede a delay (a backup voice synthesising), so they show for
~3.5 s instead of the default 1.4 s toast (show_toast gained an optional
duration_ms arg; other callers are unchanged). Messages (one line):
  ElevenLabs -> Gemini, out of credits : "Premium voice out of credits -
                                          switching to Google voice."  (warning)
  ElevenLabs -> Gemini, transient      : "Premium voice unavailable -
                                          switching to Google voice."  (info)
  -> pyttsx3, Gemini daily limit       : "Cloud voices maxed out - using the
                                          offline voice."              (warning)
  -> pyttsx3, transient                : "Cloud voices unavailable - using the
                                          offline voice."              (info)
  Deepgram streaming -> Google batch   : "Live transcription dropped - using
                                          backup recognition."         (warning)

Manual test (hard to force naturally): the easiest is to break a key. Temporarily
put a bad ELEVENLABS_API_KEY in .env (keep a valid GEMINI_API_KEY), restart, and
say anything -> you should SEE one toast "Premium voice ... switching to Google
voice" on the first reply, then hear the Gemini voice, and NO further toast on
later replies (deduped). Restore the key after. (Same idea for Gemini->pyttsx3
by breaking both cloud keys; for the STT toast, break the Deepgram key while
stt_provider="deepgram" -> first listen toasts "Live transcription dropped".)

Code-level proof - from repo root:
  uv run pytest -q tests/test_provider_notices.py
  -> _announce_switch fires once per target (gemini/local) with quota->warning
     vs transient->info wording; _announce_stt_fallback fires once. (Captured by
     connecting a slot to signals.notice; no QApplication loop needed.)


-----------------------------------------------------------------------
Command bar - placeholder / text vertical alignment
-----------------------------------------------------------------------
Background: the directive input (_TagLineEdit, a QTextEdit locked to a ~48px
two-line box) drew its single line of text/placeholder at the TOP of the box, so
"AWAITING DIRECTIVE..." floated above the centered ">_" prompt and mic. Fix:
_apply_vcenter() sets a top viewport margin to vertically center the text while
it's a single line (placeholder + short commands), and drops to top-align (0)
once it wraps to two lines so both stay visible. Recomputed on resizeEvent (the
widget's real height isn't known at __init__) and on every reflow.

Manual test: launch JARVIS. The "AWAITING DIRECTIVE..." placeholder should sit
vertically centered, in line with ">_" and the mic icon. Type a short command ->
still centered. Type/paste a long command that wraps to two lines -> both lines
visible (text top-aligns, no clipping). Voice dictation text should also sit
centered as it streams in.

(No unit test - pure Qt geometry; verified by eye + an offscreen construction
check that placeholder/1-line center and wrapped text top-aligns.)


-----------------------------------------------------------------------
Streaming STT - keyterm boosting (cut mishears on JARVIS/command words)
-----------------------------------------------------------------------
Background: live runs showed Deepgram mishearing JARVIS-specific words -
"Jarvis" -> "Denise"/"Ray Jarvis", brand/app names, etc. nova-3 supports
"keyterm prompting": a short list of terms it biases recognition toward. We now
build that list and pass it on every streaming connect.

The list (core/stt/factory.build_keyterms) = the wake word (config.wake_word) +
your name (config.user_name) + a small built-in base of brand/proper nouns
(Chrome, Edge, Firefox, Spotify, GitHub, YouTube, Gmail, VS Code, Notepad,
Wikipedia, Reddit, Deepgram) + your own config.stt_keyterms. It is NOT a giant
hardcoded blob: common words ("volume", "weather") are deliberately excluded
(recognised fine; would dilute the boost), the list is deduped case-
insensitively and capped at 50, and you extend it yourself via stt_keyterms.

Scope: keyterms fix a BOUNDED vocabulary (wake word, command/brand words, names
you add). They do NOT fix arbitrary open-ended proper nouns - e.g. a random city
you never listed. Add cities/names you say often to stt_keyterms to bias those.
Only sent on nova-3 (the connect param is nova-3 only); ignored on other models.

Config: new `stt_keyterms` (list) in config/jarvis.json, default []. Example:
    "stt_keyterms": ["Kuwait", "Mosul", "Valentine", "auth-rewrite"]

Manual test (stt_provider="deepgram"): say "Jarvis, what's the time" a few times
- "Jarvis" should land far more reliably than before. Add a name you say often
to stt_keyterms, restart, and it should transcribe more consistently too.

Code-level proof - from repo root:
  uv run pytest -q tests/test_stt_streaming.py
  -> build_keyterms leads with wake word + user name, includes the base, excludes
     common words, dedupes case-insensitively, and tolerates a bare config;
     _connect_params attaches `keyterm` ONLY for nova-3 with a non-empty list.


-----------------------------------------------------------------------
CI - Windows runner added to the test matrix
-----------------------------------------------------------------------
Background: CI ran tests on ubuntu-latest only, despite JARVIS being a
Windows-targeted app (MCI audio, pycaw mute probe, powershell/cmd exec, SAPI
voices) - none of that platform code was exercised in CI. .github/workflows/
test.yml now runs a matrix: ubuntu-latest x {3.11, 3.12} + windows-latest x 3.12.
The apt-get system-deps step is gated `if: runner.os == 'Linux'` (Windows ships
the Qt plugins + a bundled PortAudio wheel, so no system packages needed).

Verify: open the GitHub Actions "tests" workflow on the latest push - there
should now be a "pytest (windows-latest - Python 3.12)" job, green, running the
same `pytest -q -m "not integration and not qt and not slow"` under
QT_QPA_PLATFORM=offscreen. Locally on Windows that selection is 357 passed,
2 deselected.


-----------------------------------------------------------------------
Streaming STT - persistent Deepgram socket (opt-in, kills the CONNECTING gap)
-----------------------------------------------------------------------
Background: each listen turn opened a fresh Deepgram websocket (~1s connect),
which is the whole reason the CONNECTING state exists. New opt-in config
`stt_persistent` (default False) keeps ONE socket open across turns and warms it
with KeepAlive between them, so each turn skips the connect - capture is
near-instant.

How it works (researched against Deepgram docs): the socket opens on the FIRST
turn (audio must flow before KeepAlive can hold it open), a keepalive thread
pings every 5s while idle (Deepgram drops idle sockets after ~10s, NET-0001),
and each turn ends with Finalize (flush + final result, socket stays open)
instead of CloseStream. begin_turn/end_turn replace start/finish on the reused
session; all websocket sends are serialised under one lock so the writer,
keepalive, and finalize threads never interleave frames.

Safety (3 layers, no regression risk): default False = today's fresh-socket-
per-turn path, byte-for-byte. When True, if the warm socket is unhealthy at turn
start it's discarded and a fresh per-turn socket opens; if THAT fails, the Google
batch fallback still catches it. A stream error mid-turn drops the warm socket so
the next turn reopens clean. The socket is closed gracefully on app exit
(closeEvent -> voice_engine.close_persistent()).

Manual test (needs DEEPGRAM_API_KEY): set stt_provider="deepgram" AND
stt_persistent=true in config/jarvis.json, restart. First turn: log shows
"Deepgram persistent socket open" once. Subsequent turns: NO new "socket open"
line, and the CONNECTING phase should be much shorter / barely visible - you can
basically talk right away. Leave it idle > 10s between turns and it should still
work (keepalive held it). Flip stt_persistent back to false to compare.

Code-level proof - from repo root:
  uv run pytest -q tests/test_stt_streaming.py tests/test_voice_streaming.py
  -> persistent reuses one socket across turns (opens once), Finalizes WITHOUT
     closing, stays is_alive() between turns, pings KeepAlive while idle, and
     CloseStream only on close(); VoiceEngine drives begin_turn/end_turn (not
     start/finish) in persistent mode and leaves the socket warm. (Websocket +
     mic faked.)


-----------------------------------------------------------------------
Esc-to-interrupt JARVIS
-----------------------------------------------------------------------
Background: once a command was submitted (typed or voice) there was no way to
abort it. Now a global Esc (QShortcut, Qt.ApplicationShortcut in window.py)
interrupts whatever's in flight: it bumps _transcript_update_token (the existing
staleness guard - drops the pending brain reply, follow-up narration, and stale
TTS callbacks), SILENCES the voice mid-sentence, cancels a live mic capture,
stands down a pending confirmation, marks the in-flight command's existing
sys-log row as an amber "Interrupted" (UPDATES the row - no duplicate - and
stops the typewriter so its "thinking" dots can't clobber the marker), and
restores the prompt to the command bar (only if it's empty - won't clobber text
you've started typing). A bare listening-cancel logs nothing (no command was
submitted). Handler: ui/main_window/interrupt_mixin.py.

How instant voice-stop works: TtsEngine gained a generation counter. The Windows
MCI players (core/tts_elevenlabs.play_mp3_stream / play_mp3_bytes, core/tts_gemini
._play_wav_via_mci) no longer block on `play ... wait`; they use the shared
_mci_play_until_done() - non-blocking `play` + a ~40ms poll loop on the SAME
worker thread, which issues `stop` the moment should_stop() flips. stop_speaking()
bumps the gen (current clip stops) and drains the queue; pyttsx3 falls back to
engine.stop(). No cross-thread MCI calls.

Known limit (by design): dispatch() runs synchronously on the Qt main thread, so
an action ALREADY executing (e.g. a browser step) blocks the event loop and Esc
can't fire until it returns. Interrupt covers thinking / speaking / listening /
awaiting-confirmation - not an action mid-run.

Manual test (uv run python main.py):
  1. Type a command, press Esc while it's "thinking" -> nothing runs, the prompt
     is back in the input, sys-log shows it + amber "Interrupted".
  2. Ask something with a long spoken reply, press Esc mid-sentence -> audio cuts
     INSTANTLY.
  3. Trigger a listen (wake word / mic button), press Esc while LISTENING ->
     capture aborts, back to idle, no "no speech" toast.
  4. Trigger a destructive action's confirm card, press Esc -> cancelled, logged.
  5. Press Esc while idle -> nothing happens. With the tag-suggestion popup open,
     Esc closes the popup first (second Esc would interrupt).
  6. Start typing a NEW command while JARVIS speaks, press Esc -> voice stops, your
     in-progress text is NOT clobbered.

Code-level proof - from repo root:
  uv run pytest -q tests/test_interrupt.py
  -> stop_speaking() bumps the gen + drains the queue (firing each on_done,
     is_speaking->False); _mci_play_until_done issues `stop` on a should_stop
     signal and otherwise runs to completion (faked mci); cancel_listening makes
     _run_streaming_stt deliver nothing; the interrupt handler restores the prompt
     only when the field is empty, logs the row, bumps the token, and idles.


-----------------------------------------------------------------------
code_execution runs off the Qt main thread (no more UI freeze)
-----------------------------------------------------------------------
Background: code_execution used to run synchronously inside dispatch() on the Qt
main thread, so any shell/python/pip command froze the whole UI for the length of
the subprocess (up to its timeout) and the "live" terminal stream only appeared
all at once at the end. It now runs on a daemon worker thread (jarvis-code-exec),
exactly like document_creation. The result returns via signals.code_execution_done
(Qt.QueuedConnection) to _on_code_execution_done on the main thread, which either
finishes the command or shows a confirm card. A confirm card's "Yes" re-enters the
SAME worker helper (_spawn_code_worker) via the new _confirm_mode "executor_code",
so the post-confirm re-run (resolve_confirmation -> _stream_execute) also stays off
the main thread. browser_automation and automation_task/run_workflow are UNCHANGED
- they still run synchronously on the main thread (Playwright's sync API is
thread-affine to the thread that started the browser).

A new in-flight guard (_code_exec_in_flight) blocks a second command while one is
running (toast: "A command is running - please wait."). An owner token
(_code_exec_flight_token) ensures an orphaned post-Esc worker can't clear a newer
command's guard.

Esc + ghost output: Esc releases the guard immediately (UI accepts new commands at
once) and calls code_exec.cancel_active_execution(), which (a) bumps an execution
generation so the orphaned worker's reader thread STOPS painting terminal lines and
the exit-code badge - no ghost output after you cancel - and (b) [Phase 3] KILLS the
running foreground subprocess tree (_kill_process_tree) so a long command stops at
once instead of running on in the background. _stream_execute registers its live
Popen in a lock-guarded handle while running and clears it on exit.
Known limit (by design): the blocking subprocess.run() branches - run_powershell,
run_cmd, install_package - expose no Popen handle, so Esc can't kill THOSE mid-run;
they run to their own timeout (60s / 60s / 120s). All the streaming commands
(run_shell, run_python, run_script, git, npm, AI plans/fixes) ARE killed.

Manual test (uv run python main.py):
  1. "run uv run pytest" (or any long command) -> the UI STAYS RESPONSIVE while it
     runs, an info TOAST shows the brain's ack (e.g. "Firing up pytest...") as the
     "running" cue, and terminal output STREAMS LIVE line-by-line (not all at once
     at the end). NOTE: the left-panel status_lbl/hud_status/state_pill are HIDDEN
     compat widgets (dashboard.py) - the visible cues are the toast + the streaming
     terminal, NOT a status line.
  2. While that command is still EXECUTING (terminal actively streaming - not the
     earlier "thinking" phase), type another command -> amber toast "A command is
     running - please wait."; the second command does not start. (During the THINKING
     phase a second command instead SUPERSEDES the first via token staleness - only
     one ever runs; that's expected, not the in-flight block.)
  3. "use PowerShell to list my desktop" -> a confirm card appears; click Yes ->
     it runs OFF the main thread (UI stays live) and reports back.
  4. Start a long command (e.g. "run python -c \"import time; time.sleep(30)\"" or
     "run uv run pytest"), press Esc mid-run -> UI returns to idle immediately, the
     command bar gets the prompt back, NO further terminal lines appear, AND the
     subprocess is actually KILLED (confirm in Task Manager - no lingering python /
     pytest process). Contrast: a PowerShell/CMD/pip command can't be killed mid-run
     and runs to its timeout.
  5. "run git status" -> fast path, finishes normally, output shown.

Code-level proof - from repo root:
  uv run pytest -q tests/test_code_exec_worker.py
  -> off-thread spawn sets the guard + flight token and runs the callable on a
     worker thread; a final result finishes once and clears the guard; a
     needs-confirmation result shows the card (mode "executor_code"), holds the
     guard, and stashes the brain context; a single- AND multi-hop confirm
     round-trip re-enters _spawn_code_worker each hop and clears the guard only on
     the final result; auto-confirm re-spawns off-thread (never resolves on main);
     _process_cmd rejects a new command while in flight; a stale (Esc-superseded)
     token drops the result without finishing and only the owner releases the
     guard; the cancelled execution generation suppresses the terminal stream
     while a normal run still streams; and [Phase 3] cancel_active_execution()
     kills the registered foreground subprocess (a 30s sleep returns in well under
     15s), _clear_foreground only clears the matching proc, and cancel with nothing
     running just bumps the generation.


-----------------------------------------------------------------------
Configurable per-command timeout (code_execution)
-----------------------------------------------------------------------
Background: every code_execution action had a hardcoded timeout (run_python 30s;
run_shell/git/npm/run_script/powershell/cmd 60s; install_package 120s). A genuinely
long command (full test suite, a big pip install) would hit the default and get
killed mid-run. The brain can now pass a `timeout` parameter (seconds) to override
the default per command. It's coerced (str/float -> int) and clamped to 1-3600s;
a missing/blank/invalid/<=0 value falls back to the action default. Timeout
messages now reflect the actual limit used (e.g. "timed out after 300s").

The brain sets `timeout` when the user names a limit ("...with a 5 minute timeout"
-> 300) or for a known-long command ("run the full test suite", "pip install
torch" -> a generous value). See CLAUDE.md code_execution parameters.

Manual test (uv run python main.py):
  1. "run python that sleeps for 20 seconds with a 2 second timeout" (or just
     "run python -c \"import time; time.sleep(20)\" with a 2 second timeout")
     -> it's killed at ~2s and reports "timed out after 2s", NOT the 30s default.
  2. "run uv run pytest with a 5 minute timeout" -> the brain emits timeout: 300;
     a suite that takes >60s now completes instead of being cut off at the old
     60s default.
  3. A normal command with no timeout phrasing -> behaves exactly as before
     (action default applies).

Code-level proof - from repo root:
  uv run pytest -q tests/test_code_exec_worker.py
  -> _resolve_timeout returns the default for missing/blank/invalid/<=0 and the
     coerced+clamped value otherwise (str/float coercion, 1-3600 clamp);
     _stream_execute enforces an arbitrary short timeout and the message reflects
     it; and a brain-supplied `timeout` flows through _handle_code_execution to
     kill run_python at the requested limit (1s) rather than the 30s default.


-----------------------------------------------------------------------
Shell scripting logic (chaining + search routing) in generated commands
-----------------------------------------------------------------------
Background: nothing told JARVIS how to chain shell commands per shell, and two
real gaps existed - run_shell uses shlex + shell=False so &&/||/;/| are NOT
interpreted (they become literal args and break), and Windows PowerShell 5.1
treats && / || as a PARSE ERROR. Guidance now lives in three spots that generate
shell: CLAUDE.md S11 (the brain), _nl_to_command (Haiku) and _plan_steps (Sonnet)
in core/handlers/code_exec.py. A dev-side CLAUDE.local.md carries the same rules
for Claude Code itself (loaded by Claude Code, NOT by JARVIS's brain.py).

Rules encoded:
  - run_cmd / bash chain with && (on success) and || (fallback).
  - run_powershell NEVER uses && / || (PS 5.1 parse error) -> use ; and
    'if ($?) { ... }' (run-on-success), 'if (-not $?)' / try-catch (fallback).
  - A command that must chain routes to run_cmd or run_powershell, NOT run_shell.
  - Defensive guards/fallbacks WHEN RELEVANT, not on trivial one-shot commands.
  - Searching file CONTENTS -> file_operation/find_in_files; by name ->
    search_files. Raw shell search only when explicitly asked, and then the right
    tool per shell: bash grep, PowerShell Select-String (never grep), CMD findstr.

These paths use live LLM calls, so they are verified MANUALLY, not by pytest.

Manual test (uv run python main.py):
  1. "use PowerShell to make a folder build and then move into it" -> run_powershell
     using ';' / 'if ($?)' (NOT &&).
  2. "in cmd, build the project then run it only if the build passes" -> run_cmd
     using &&.
  3. "create a folder and cd into it" (no shell named) -> a chaining-capable action
     (run_powershell/run_cmd), NOT run_shell with a literal && that would break.
  4. "find the word baseline in my project files" -> file_operation/find_in_files
     (content grep), NOT a raw shell grep.
  5. "use PowerShell to search the logs for the word timeout" -> run_powershell with
     Select-String (not grep), fallback when natural.
  6. "run git status" -> still a single plain command (no over-engineering).


-----------------------------------------------------------------------
Browser-free inline workflows run off the Qt main thread (no UI freeze)
-----------------------------------------------------------------------
Background: automation_task/run_workflow dispatched synchronously on the Qt main
thread; _run_from only kept the UI alive by pumping processEvents() between steps,
so a multi-clause command (e.g. "create a folder in tests AND a file inside it")
briefly froze the window ("Not Responding"). An INLINE workflow whose EVERY step is
a structured dict with a Playwright-free / Qt-free intent now runs on the SAME
worker harness as code_execution (worker -> code_execution_done -> main-thread
finish/confirm-card -> confirm re-spawns the worker). Anything that could touch the
browser stays on the main thread, unchanged.

Eligibility (execution_mixin._workflow_is_offthread_safe) - ALL must hold:
  - intent=automation_task, action=run_workflow, non-empty parameters.steps.
  - EVERY step is a structured dict (NOT a natural-language string - those resolve
    mid-run and could become a browser action).
  - EVERY step's intent is in the whitelist _OFFTHREAD_WORKFLOW_INTENTS =
    {file_operation, system_control, code_execution, reminder_task, type_text,
    control_mouse, read_screen, weather} (all vetted Qt-free / Playwright-free:
    pyautogui + pyperclip for input, pyautogui+pytesseract for OCR, network for
    weather).
Anything else (NL string step, browser_automation/open_app/open_url/search_web,
vision_analysis [its source="browser" path uses Playwright], or unvetted intents
like jarvis_meta/document_creation) -> unchanged synchronous main-thread path.

Also: _yield_ui() (automation_handler) is now a no-op off the main thread (the main
loop pumps itself; processEvents must only run on the owning thread), and
_workflow_depth is lock-guarded (read on main, mutated on the worker).

Manual test (uv run python main.py):
  1. "create a folder called temp inside the tests folder and then create a file
     readme.txt inside it" -> runs on the worker; the UI STAYS RESPONSIVE (no
     "Not Responding"); the create_directory/create_file confirm cards appear and,
     on Confirm, the workflow CONTINUES off-thread to completion.
  2. "create tests/x.txt and mute the volume" -> two-step file+system workflow, smooth.
  3. "open chrome and search for lofi" -> UNCHANGED: stays on the main thread (browser
     steps), no Playwright "cannot switch thread" error.
  4. A saved workflow run by name -> UNCHANGED (synchronous; no inline steps to classify).
  Clean up any tests/temp, tests/x.txt artifacts afterward.

Code-level proof - from repo root:
  uv run pytest -q tests/test_workflow_offthread.py
  -> _workflow_is_offthread_safe returns True only for all-structured whitelist-only
     steps and False for an NL-string step / browser step / non-whitelist intent /
     empty steps; an eligible inline workflow routes to _spawn_code_worker (not inline
     dispatch) while an ineligible (browser) one dispatches synchronously; and
     _yield_ui pumps on the main thread but is a no-op on a worker thread.


-----------------------------------------------------------------------
Crash diagnostics (silent-exit visibility)
-----------------------------------------------------------------------
Background: the app could vanish to the shell prompt with NO Python traceback
(only the benign "Could not parse stylesheet" lines), which masks the cause -
a native segfault, a Qt thread-affinity violation, an unhandled exception on a
worker thread, or the OS killing the process for memory. app.py now installs
_install_crash_diagnostics() at startup: faulthandler (native dumps),
sys.excepthook + threading.excepthook (main AND worker thread exceptions), and
qInstallMessageHandler (Qt WARNING/CRITICAL/FATAL - e.g. "Cannot create children
for a parent in a different thread"). All of it writes to stderr (terminal) AND
appends to logs/crash.log (gitignored). Benign stylesheet spam is filtered out.

How to use when the app dies unexpectedly:
  1. Reproduce, then read logs/crash.log (or the terminal).
  2. crash.log HAS a new entry (traceback / "Qt FATAL" / a thread name) -> a real
     code crash; the entry points at the offending call.
  3. crash.log stays EMPTY after a crash -> no Python/Qt fault fired = the OS
     killed the process (almost always memory pressure). Remedy is headroom
     (close browser engines / pytest / other apps), not a code fix.

Note: worker threads now in play (code_execution + browser-free workflows off the
main thread) make threading.excepthook especially useful - an exception inside a
worker that used to die quietly is now logged with its thread name.


-----------------------------------------------------------------------
Deterministic file/folder-creation routing guardrail
-----------------------------------------------------------------------
Background: the brain is non-deterministic about routing plain "create a folder
and a file" (NO shell named) - across runs it produced automation_task, single
file_operation (dropping the file), and run_powershell. Prompt nudges couldn't pin
it. parse_file_creation_command (core/workflow_nlu.py) is now a deterministic
fast-path, wired into ask_claude right after parse_create_workflow_command (BEFORE
any model call), so common file-creation phrasings route to file_operation every
time and never reach run_powershell.

Scope (conservative - bails to the model on anything it can't cleanly parse):
  - Folder + file ("create a folder X [in Y] and [then] a file Z inside it")
    -> automation_task with create_directory + create_file (file nested in folder).
  - Folder only -> file_operation/create_directory.
  - File only ("create a file Z [in Y]") -> file_operation/create_file (empty).
  - BAILS (returns None -> model handles) when: a shell is named (powershell/cmd/
    bash/...), there's dictated content ("with content", "that says", "write ..."),
    the name has spaces, or the phrasing isn't a clean create. Respects @tags -
    only fires for no-tag / @files / @automate.

Manual test (uv run python main.py):
  1. "create a folder called temp inside the tests folder and then create a file
     readme.txt inside it" -> NOW DETERMINISTIC: [brain] shows INTENT automation_task
     with file_operation steps (tests/temp + tests/temp/readme.txt), never
     run_powershell. Runs off-thread (browser-free workflow). Confirm cards appear.
  2. Run it several times -> same routing every time (the whole point).
  3. "use PowerShell to create a folder build and a file inside it" -> still
     run_powershell (shell named -> guardrail bails).
  4. "create a file notes.txt with content hello" -> guardrail bails; model routes.

Code-level proof - from repo root:
  uv run pytest -q tests/test_file_creation_routing.py
  -> folder+file -> 2-step file_operation workflow with correct nested paths;
     folder-only / file-only -> single file_operation; shell-named / dictated-content
     / spaced-name / non-creation all bail to None; and ask_claude returns the
     file_operation result with NO model call for a matching prompt.


-----------------------------------------------------------------------
Selective memory deletion (jarvis_meta/forget_memory)
-----------------------------------------------------------------------
Background: wipe_memory was all-or-nothing (ConversationMemory.clear() nukes the
whole data/memory.jsonl). forget_memory deletes only the exchanges about ONE
topic, keeping the rest. core/memory.py gains find_exchanges(query) (case-
insensitive substring match over each user+assistant PAIR -> [{user, assistant,
preview}]) and forget_exchanges(matches) (delete-by-identity, robust to new
exchanges added since, so the 'forget X' command itself isn't caught). The meta
handler previews the matches in a CONFIRM card before deleting (handler owns the
card; requires_confirmation stays false in JSON).

Routing: "forget everything / our whole conversation / start fresh" -> wipe_memory
(unchanged). "forget what my brother said about X / forget the part about X /
delete the memory about X" -> forget_memory with query=X.

No self-trace: brain.py skips add_exchange for jarvis_meta forget_memory/wipe_memory
(_is_memory_meta_command), so the "forget X" / "wipe" request itself is NOT written
to memory.jsonl. Without this the request re-introduced the topic (e.g. "forget ...
porn movies" got logged and then self-matched the forget preview) — see image where
the preview showed 2 matches incl. the user's own command. Now the preview shows
only the real target(s).

No false FAILED: _execute_result no longer calls memory.inject_outcome for a
needs_confirmation (PENDING) result — previously a confirm-required command logged a
misleading "[Result: … → FAILED: failed]" the moment the card appeared (and, for
memory-meta commands not in history, appended to the WRONG message). Genuine
failures (non-zero exit / real error) are still recorded.

Preview count == removed count: that same stray "[Result: …]" marker also caused
"preview 2 / removed 1" — inject_outcome appended a marker to a matched assistant
message BETWEEN find_exchanges (captured content without it) and forget_exchanges
(saw it with it), so the exact-content identity match missed one pair. Two-layer
fix: (a) the no-FAILED fix above stops the mutation; (b) forget_exchanges now
strips [Result: …] markers before comparing, so the match is robust regardless.

Manual test (uv run python main.py):
  1. With the brother's "porn movies" search still in history, say
     "forget what my brother said about porn movies" -> a confirm card PREVIEWS
     the matched exchange(s) ("Forget 1 exchange about 'porn movies'? ...") ->
     Confirm -> JARVIS reports it removed N exchange(s). The porn exchange is gone;
     ask something that relied on OTHER earlier context and it still remembers.
     (Or inspect data/memory.jsonl: the porn lines are gone, the rest remain.)
  2. "forget about something we never discussed" -> "No memory about '...' found."
  3. "forget everything" -> wipe_memory still clears ALL (unchanged).

Code-level proof - from repo root:
  uv run pytest -q tests/test_memory_forget.py
  -> find_exchanges matches only the relevant pair (case-insensitive, user OR
     assistant content), returns [] on no match; forget_exchanges removes only the
     matched pair, keeps the rest, persists to disk, and is identity-based (a later
     same-topic exchange survives); handler returns needs_confirmation on a match,
     a success no-op on none, and an error on an empty query.


-----------------------------------------------------------------------
Debug/UX polish: forget preview, confirm logging, colored logs, clean quit
-----------------------------------------------------------------------
Four improvements from a live-test review:

1. Forget preview shows WHY it matched. find_exchanges (core/memory.py) now
   returns matched_in ("user"|"assistant") and a preview that, for an assistant-
   only hit, appends '← matched in reply: "…snippet…"'. So a casual greeting that
   matched only because JARVIS's REPLY mentioned the topic is no longer a mystery
   in the confirm card. Manual: a greeting whose reply said "your brother…" shows
   the reply snippet in the forget card.

2. Executor confirmations now logged. request_confirmation (core/handlers/
   shared.py) prints "[confirm] executor confirmation requested → <prompt>".
   Resolves the "[brain] CONFIRM: False but a modal appeared" confusion — that
   modal is the EXECUTOR's confirmation (a separate layer from the brain's
   requires_confirmation field). Manual: trigger forget_memory / a file delete →
   a [confirm] line appears alongside the [brain] block.

3. Readable debug. core/log.py colorizes each tag (lightweight ANSI, no new
   dependency — auto-disabled when stdout isn't a TTY) and prints a blank line
   whenever the tag changes, so [brain]/[tts]/[voice] groups don't run together.
   Manual: run the app in a real terminal and confirm colored, spaced blocks.

4. Clean quit (EPIPE fix). quit_application now calls self.close() (was
   QApplication.quit()), so closeEvent runs the FULL shutdown — browser.stop(),
   wake detector, hotkeys, Deepgram socket, history. app.quit() skipped closeEvent,
   abandoning Playwright's node driver → it wrote to a closed pipe on teardown
   ("Error: EPIPE: broken pipe"). Manual: say "close jarvis" / "bye" with a browser
   engine open → app exits with NO node EPIPE dump in the terminal.

Code-level proof:
  uv run pytest -q tests/test_memory_forget.py
  -> includes the preview/match-context assertions (matched_in + "matched in
     reply"). The logger, confirm-line, and quit path are verified manually (UI /
     terminal behavior).


-----------------------------------------------------------------------
Mic stops by itself right after JARVIS speaks (persistent-socket stale end)
-----------------------------------------------------------------------
Background: with stt_persistent=True one warm Deepgram socket is reused across
turns. end_turn() sends Finalize but keeps the socket + callbacks; Deepgram's
response (a final + UtteranceEnd) can arrive AFTER the next begin_turn() swapped in
the new turn's callbacks, so the PRIOR turn's UtteranceEnd fires the new turn's
_on_utt_end → done.set() → the mic closes after ~22 frames having captured nothing.
Symptom: open the mic right after JARVIS replies → it turns off by itself; retrying
1-2x works once the stale event has cleared.

Fix (core/voice.py _run_streaming_stt._on_utt_end): ignore an UtteranceEnd that
arrives before this turn has heard ANY speech (got_speech not set) — that's the
stale leftover. A genuinely silent turn is still ended by the no-speech timeout, so
nothing hangs. On a persistent socket it logs
"[voice] ignored stale UtteranceEnd before speech (warm socket)".
NOT done: a time-based "grace window" was considered and dropped — it risked
clipping the start of fast speech; the got_speech guard is the safe, robust fix.
Remaining minor edge (unobserved): a stale FINAL with text leaking into the new
turn — would need a turn-epoch; revisit only if a previous command ever re-runs.

Manual test (uv run python main.py, stt_persistent on):
  Say something → let JARVIS reply → IMMEDIATELY open the mic and speak. Repeat 5x.
  PASS: every attempt captures first-try; [stt] shows full frame counts (100+), not
  ~22-frame stops; you may see "[voice] ignored stale UtteranceEnd before speech".
  (Empty mic still closes on the no-speech timeout — correct, just not instant.)

Code-level proof:
  uv run pytest -q tests/test_voice_streaming.py
  -> test_persistent_ignores_stale_utterance_end_before_speech: a stale UtteranceEnd
     before speech is ignored and the real utterance ("open chrome") is still
     captured; without the guard it would be empty.


-----------------------------------------------------------------------
Double-listen re-entry guard (core/voice.py listen())
-----------------------------------------------------------------------
Bug: listen() spawned a capture thread with NO re-entry check, so it could be
entered again while a capture was already live. Two mic streamers then fed the
ONE warm Deepgram socket → audio captured twice (e.g. "Alright. Bye. See you
next time." transcribed twice) and garbled frames. Seen in the log as two
"[stt] mic streamer started" with no "stopped" between them.
Why _listening couldn't be the guard: the HUD state machine calls
clear_listening() whenever it leaves the "listening" state (it's a mic-icon
flag), so it can be cleared mid-capture — unreliable for gating re-entry.

Fix: a dedicated guard owned solely by the listen lifecycle —
self._capture_active (bool) under self._capture_lock. listen() does an atomic
test-and-set: if a capture is already active it logs
"[voice] listen() ignored — capture already in flight" and returns; otherwise it
sets the flag and spawns the thread. _listen_thread clears the flag in its
finally. _listening (the HUD icon flag) is untouched and keeps its old behaviour.

Manual test (uv run python main.py):
  Speak a command → let JARVIS reply → rapidly trigger listening again while the
  prior turn may still be finishing. Repeat 5x.
  PASS: no command is captured/executed twice; at most one
  "[stt] mic streamer started" is live at a time; you may see
  "[voice] listen() ignored — capture already in flight" when re-entry is blocked.

Code-level proof:
  uv run pytest -q tests/test_voice_streaming.py
  -> test_second_listen_ignored_while_capture_in_flight: a 2nd listen() while one
     is live (and even after clear_listening()) spawns nothing — only the first runs.
  -> test_listen_runs_again_after_previous_capture_finishes: once the guard clears,
     the next listen() proceeds normally.


-----------------------------------------------------------------------
STT endpointing tuning — clamp + settle window (F)
-----------------------------------------------------------------------
Two parts.

1) Clamp (core/stt/deepgram.py): endpointing_ms and utterance_end_ms were sent to
   the API verbatim, so a config typo (e.g. 5ms or 99999ms) could silently wreck
   capture. Now clamped: endpointing -> [10, 2000], utterance_end -> [1000, 5000]
   (1000 is Deepgram's floor). Pure safety; valid values pass through unchanged.

2) Settle window (config.stt_settle_ms, default 0 = OFF; core/voice.py
   _run_streaming_stt): previously the turn committed the instant the FIRST
   UtteranceEnd fired after speech — so a >=1s think-pause mid-command
   ("open chrome ... and search cats") cut you off on the first half. With
   stt_settle_ms > 0, an UtteranceEnd ARMS a timer instead of committing; if speech
   resumes within the window the on_partial/on_final handlers clear it and the turn
   continues; if the window elapses silent, it commits (logs
   "[voice] streaming: settled after pause — committing"). Costs ~settle_ms latency
   per command, so it's opt-in; 0 preserves the proven snappy default.

Manual test (uv run python main.py, stt_provider="deepgram"):
  Default (stt_settle_ms=0): everything behaves exactly as before — snappy commits.
  Set stt_settle_ms=400 in config/jarvis.json, restart. Say a command, pause ~0.7s
  mid-sentence, then finish ("open chrome <pause> and search for cats").
  PASS with settle on: the whole phrase is captured as one command.
  PASS with settle off (0): the phrase is cut at the pause (first half only) — the
  difference is the point of the feature.

Code-level proof:
  uv run pytest -q tests/test_voice_streaming.py tests/test_stt_streaming.py
  -> test_settle_window_keeps_turn_open_through_midcommand_pause: settle_ms=300, a
     pause+resume captures "open chrome and search cats".
  -> test_no_settle_commits_on_first_utterance_end: settle_ms=0, same session is cut
     to "open chrome".
  -> test_settle_window_still_commits_after_silence: a true stop still commits.
  -> test_endpointing_values_are_clamped_to_safe_bounds: out-of-range ms are clamped.


-----------------------------------------------------------------------
Saved / cron workflows off the Qt main thread (D)
-----------------------------------------------------------------------
Before: only an INLINE run_workflow (parameters.steps present) was eligible to run
on the worker thread. A saved-by-name run ("run my morning routine"), a direct
trigger, and a cron fire all arrive as parameters.task_name with NO inline steps,
so they fell through to the synchronous dispatch and ran on the Qt main thread —
freezing the UI for the whole routine.

After: _resolve_workflow_steps() loads a saved workflow's persisted steps from the
library when only task_name is present, and _workflow_is_offthread_safe() classifies
them with the SAME whitelist (_OFFTHREAD_WORKFLOW_INTENTS) as inline steps. An
all-whitelist saved/cron workflow now runs off-thread; anything with a browser/
search/open_app step, an NL-string step, or a disabled/missing workflow stays
synchronous on the main thread (Playwright's sync API is main-thread-affine), so the
handler still reports the real disabled/not-found error.

F-3 safety (IMPORTANT): because scheduled fires can now go off-thread, the off-thread
auto-confirm bypass (_on_code_execution_done) and the synchronous one (_finish_execute)
now BOTH honour result["_scheduled"] — a cron fire NEVER auto-confirms a destructive
step even with auto_confirm ON; it always shows the card. (Previously only the
brain-level requires_confirmation bypass checked _scheduled.)

Manual test (uv run python main.py):
  1) Save a browser-free workflow (e.g. mute + create a folder + a reminder). Run it
     by name ("run <name>"). PASS: the UI stays responsive while it runs (you can move
     the window / it doesn't freeze); the workflow completes; transcript shows steps.
  2) Save a workflow that opens Chrome / navigates. Run it by name. PASS: still works
     (runs synchronously on the main thread — Playwright is happy), no thread crash.
  3) Turn auto-confirm ON. Give a SCHEDULED workflow a delete step and let the cron
     fire (or set a near-future cron). PASS: a confirmation CARD appears — the cron
     does NOT silently delete. Same workflow run manually with auto-confirm ON does
     auto-confirm (the difference is the _scheduled flag).

Code-level proof:
  uv run pytest -q tests/test_workflow_offthread.py
  -> test_saved_workflow_steps_resolved_and_eligible / _with_browser_step_is_ineligible
     / _with_string_step_is_ineligible / _disabled_or_missing_saved_workflow_stays_synchronous
  -> test_saved_by_name_workflow_routes_to_worker: task_name-only run spawns the worker.
  -> test_scheduled_offthread_workflow_never_auto_confirms: _scheduled + auto_confirm
     shows the card, no resolve worker.
  -> test_non_scheduled_offthread_workflow_auto_confirms: control — non-scheduled still
     auto-resolves off-thread.


-----------------------------------------------------------------------
Opt-in explicit-content search gate (NSFW gate)
-----------------------------------------------------------------------
New config flag: config.safe_search_confirm (default FALSE). JARVIS stays a
"controller, not a safety filter" — it NEVER refuses a search. When the user opts
in (safe_search_confirm=true in config/jarvis.json), an explicit/adult query gets a
confirmation card BEFORE the search runs, so a shared/family machine doesn't surface
NSFW results on a careless or misheard command. OFF = behaviour fully unchanged.

Detection (core/content_gate.py, is_explicit_query): conservative on purpose — a
missed term beats a false positive nagging a normal search. Single tokens are
word-boundary matched (so "sex" in "Essex", "anal" in "analysis", "cum" in
"cumulative" do NOT trip it) and the most ambiguous singles (nude/naked/escort/sex)
are intentionally excluded ("nude lipstick", "naked mole rat", "sex education" pass
clean). Unambiguous phrases ("porn movies", "sex tape", "nude photos") match as
substrings.

Wiring (core/handlers/app_launcher.py): _handle_search_web checks the flag; on an
explicit query it returns request_confirmation(...) whose callback runs the real
_do_search_web once the user confirms. The search logic moved to _do_search_web
unchanged, so the clean path is identical to before.

Manual test (uv run python main.py):
  Default (safe_search_confirm off): "search youtube for <anything>" runs as before.
  Set safe_search_confirm=true in config/jarvis.json, restart.
    - Say/type an explicit search -> a confirmation card appears; nothing is searched
      until you confirm; confirming runs the search normally.
    - A normal search ("kuwait weather", "essex county") runs WITHOUT a card.

Code-level proof:
  uv run pytest -q tests/test_content_gate.py
  -> test_explicit_terms_and_phrases_match / test_clean_queries_do_not_match (the
     false-positive guard), and the three handler-wiring tests (off=direct,
     on+explicit=confirm, on+clean=direct).


-----------------------------------------------------------------------
Settings UI: Pause tolerance (stt_settle_ms) + Safe search (safe_search_confirm)
-----------------------------------------------------------------------
Wired both new knobs into the Settings page (ui/settings.py) so they're toggleable
without hand-editing config/jarvis.json:
  - Voice & audio → "Pause tolerance": a 0–800 ms slider (readout shows "Off" at 0)
    bound to stt_settle_ms. Applies on APPLY_CFG; takes effect on the NEXT turn (no
    restart) since the streaming loop reads config.stt_settle_ms per turn.
  - Behaviour & visuals → "Safe search": a ToggleSwitch bound to safe_search_confirm.
    Applies on APPLY_CFG; read per search call, so no restart.
Both mark the page dirty (● UNSAVED) on change and persist via config.save() (asdict,
so new dataclass fields persist automatically).

Manual test (uv run python main.py → Settings):
  - Drag "Pause tolerance" to e.g. 350, hit APPLY_CFG → SAVED ✓. Speak a command with
    a short mid-sentence pause → the whole phrase is captured (was cut off at 0/off).
  - Flip "Safe search" ON, APPLY_CFG. An explicit search now shows a confirm card;
    a normal search does not. Flip OFF → explicit searches go straight through.
  - Reopen Settings → both controls reflect the saved values (read from config).

Code-level proof (full-GUI test — opt-in, see note):
  JARVIS_RUN_QT=1 uv run pytest -q tests/test_settings_knobs.py
  -> test_new_knobs_read_initial_values_from_config: slider + toggle init FROM config.
  -> test_apply_writes_new_knobs_to_config: APPLY_CFG writes both back to config.
  -> test_settle_readout_shows_off_at_zero: 0 → "Off", else the ms value.

NOTE on qtgui tests: a full-GUI QApplication built in-process alongside the
thread-spawning rest of the suite segfaults, so qtgui-marked tests are SKIPPED in the
default run and opt-in via JARVIS_RUN_QT=1 (same pattern as integration tests). Plain
`qt` tests (lightweight QCoreApplication, e.g. test_workflow_thread_hop) still run in
the default suite.


-----------------------------------------------------------------------
Theme switching actually works (full proper fix)
-----------------------------------------------------------------------
Before: the Settings theme swatches saved config.theme but NOTHING applied it.
ui/theme.py was a single static cyan palette; config.theme was never read to
restyle anything; signals.theme_changed was declared but never wired; and the
accent was hardcoded as ~162 literals (93 QSS rgba(0,229,255,a) + 55 paint
QColor(0,229,255,a) + ~14 #00e5ff) across 19 ui/ files. Picking Amber/Matrix/etc.
did nothing, even after restart.

After (applies at startup — like the wake word; switching needs a restart):
  - ui/theme.py has a palette table (_THEME_PALETTES) keyed off config.theme,
    resolved once at import -> ACCENT_RGB / ACCENT_HEX / BG + all accent-bearing
    constants & QColor helpers (CYAN, PRIMARY*, BORDER_A/B, IDLE_CYAN, _cyan(),
    _primary(), COLORS). The "cyan" branch reproduces the original values
    byte-for-byte (THEME_IS_BASE), so the default path is unchanged.
  - QSS literals (and every f-string that interpolates {CYAN}) are themed
    centrally by apply_accent(), installed over QWidget/QApplication.setStyleSheet
    by install_theme() (called once in app.py main, before widgets build). No-op
    for cyan.
  - Paint-code QColor(0, 229, 255, a) literals were rewritten to
    QColor(*ACCENT_RGB, a) across 12 files.
  - Settings: theme_helper says "Applies on restart."; APPLY_CFG emits a
    "Theme saved — restart JARVIS to apply it." notice when the theme changed.

Manual test (uv run python main.py -> Settings):
  Pick a non-cyan swatch (e.g. Amber or Matrix) -> APPLY_CFG -> you get the
  "restart to apply" toast. Restart JARVIS. PASS: the WHOLE HUD is recoloured —
  arc reactor, sys-log/transcript, bars, mic, borders, buttons, sliders, badges —
  in the new accent; no cyan left bleeding through the main surfaces. Switch back
  to Stark (cyan) + restart -> identical to the original look.

Code-level proof:
  uv run pytest -q tests/test_theme.py
  -> default theme is cyan + apply_accent is a no-op (base path unchanged);
     palette covers all 5 swatches; _resolve_theme_key falls back to cyan;
     _hexrgb/_tint correct; apply_accent substitutes rgb (both spacings)+hex+bg
     for a non-base theme; install_theme idempotent + no-op on base.
  Offscreen full-window smoke (manual): force config.theme="amber", install_theme(),
  build JarvisWindow -> a real QLabel's "rgba(0,229,255,0.5)" becomes
  "rgba(255,170,0,0.5)" and the window builds/closes cleanly.


-----------------------------------------------------------------------
Theme accent completeness — the painted/icon/asset leaks (restart model)
-----------------------------------------------------------------------
Follow-up to the theme system: a few widgets expressed the accent as a literal
that bypassed BOTH theming mechanisms (the apply_accent QSS hook AND the
QColor(*ACCENT_RGB) sweep), so they stayed cyan under a non-cyan theme. Audit
classified each accent-vs-semantic; the ACCENT ones now read the token. Semantic
colours (success green, error red, warning amber) deliberately stay fixed.

Fixed (all byte-identical under the default cyan theme):
  - dashboard.py STATES: idle/listening/thinking/wake glow -> ACCENT_HEX
    (== "#00e5ff" on cyan). speaking="#83fba5" / error="#ffb4ab" left SEMANTIC.
  - widgets/visuals.py ArcReactorWidget._state_colors: listening/thinking/wake ->
    QColor(*ACCENT_RGB). speaking/error left SEMANTIC.
  - widgets/inputs.py _TagHighlighter._CYAN (valid-@tag highlight) ->
    QColor(*ACCENT_RGB). _GOLD (invalid-@tag warning) left SEMANTIC.
  - sidebar.py icon tints: _ICON_COLOR_IDLE -> IDLE_CYAN token, _ICON_COLOR_HOVER
    -> PRIMARY (themed light tint), _ICON_COLOR_ACTIVE stays CYAN (already themed).
  - theme.py jarvis_logo_pixmap(): the logo SVG bakes the accent in 8 places that
    no token can reach; on a non-cyan theme recolor_logo_svg() replaces #00E5FF
    (case-insensitive) with ACCENT_HEX before QSvgRenderer. No-op on cyan (the
    file is read unchanged -> byte-identical).
Left untouched (confirmed SEMANTIC): StatusPip (design.py + primitives.py,
green/amber/red), _TagHighlighter._GOLD, the interrupted ⛔ amber #ffb432, the
CANCEL red button, the CONFIRM button (it's QSS -> already follows the theme).

Manual test (uv run python main.py):
  Settings -> pick a non-cyan swatch (Amber/Matrix) -> APPLY_CFG -> restart.
  PASS: the "STANDBY" badge, the arc-reactor rings, the valid-@tag highlight in
  the command bar, the sidebar icons (idle + active + hover), and the brand logo
  are ALL the new accent — no cyan left on them. The status pips next to panels
  stay green/amber/red; a failed command's reactor/error text stays red; the
  CANCEL button stays red. Switch back to Stark (cyan) + restart -> identical to
  the original look.

Code-level proof:
  uv run pytest -q tests/test_theme.py
  -> apply_accent no-op on base / substitutes on non-base; STATES glow == ACCENT_HEX
     with speaking/error semantic; _TagHighlighter._CYAN == ACCENT_RGB, _GOLD fixed;
     sidebar idle/active/hover == IDLE_CYAN/CYAN/PRIMARY; recolor_logo_svg no-op on
     base + swaps on non-base (incl. the real shipped SVG). AMBIENT-INDEPENDENT
     (tests don't assume the saved theme).
  JARVIS_RUN_QT=1 uv run pytest -q tests/test_theme.py::test_visuals_state_colours_route_to_accent
  -> reactor listening/thinking/wake == ACCENT_RGB, speaking/error semantic (qtgui).
  Offscreen amber straggler smoke (manual): builds StateLabel/reactor/_InputBlock/
  sidebar + logo under amber -> all amber, zero "0,229,255" in their QSS.


-----------------------------------------------------------------------
SYS_LOG_BUFFER redesign — Variant B (rail + gutters) + JARVIS-only chips
-----------------------------------------------------------------------
Rebuilt the dashboard sys-log (TranscriptPanel) from a flat
"[time] YOU/JARVIS: msg (intent, conf%)" stream into Variant B: each line has a
4px LEFT COLOUR RAIL encoding outcome, time|speaker|message gutters, and JARVIS
reply lines carry a chip row (intent chip · confidence chip). YOU lines stay one
dense line with NO chips. Latency is DEFERRED (no latency chip, never faked).

Render path (confirmed): TranscriptPanel._render() builds HTML and setHtml()s a
QTextBrowser. Chips proved feasible in Qt rich text — bordered/background <td>
cells render; border-radius is dropped (chips are SQUARE, not rounded). So the
rail is a 4px bgcolor <td> and chips are square bordered <td>s. No QWidget rewrite.
The HTML build is a PURE module-level fn `_build_log_html(rows, expanded, preview)`
(Qt-free → unit-tested).

Rail colours: info = themed CYAN token (follows the active theme, like the theme
work); ok = #83fba5 (green), fail = #ff6b6b (red), interrupt = #ffd166 (amber) —
the last three SEMANTIC/fixed. YOU + no-outcome JARVIS rows = info; JARVIS rows
with success True/False = ok/fail; intent=="interrupted" = amber (⛔ line preserved).

Intent chip: reuses design.py INTENT_COLOR + INTENT_LABEL via the new shared
`intent_label_color()` helper (also now used by IntentBadge — single source, no
second map). Confidence chip: muted, "{int(conf*100)}%", omitted when conf is None.

Data plumbing: `success` (bool|None) added as a 7th element of the row tuple and to
add_exchange/update_last_jarvis/append_jarvis_scheduled (default None → BACK-COMPAT;
existing callers unchanged). exec_ok is threaded transcript_payload (6th elem) ->
_on_tts_ready -> _update_transcript_if_current -> update_last_jarvis. Confirmation
prompts pass None (info). Reminders pass success=True; cron scheduled passes exec_ok.

Width: the panel was widened 340 -> 400px (dashboard.py). Only the center (Expanding)
reactor zone absorbs the +60 (≈600->540px at the 1280 minimum — still ample); the
right metrics column is fixed and unaffected; no reflow. Effective text width = 360px;
a ~50-char reply wraps to ~1-2 lines; the chip row fits on ONE line (no h-scroll);
≈3 full exchanges visible at once.

Manual test (uv run python main.py):
  Run a mix of commands and watch the left SYS_LOG:
  - YOU lines: one dense line, info (themed) rail, NO chips.
  - JARVIS success (e.g. "open chrome"): GREEN rail + chip row [intent] [conf%].
  - JARVIS failure (e.g. a search with no network): RED rail, [intent] [0%].
  - Esc mid-command: amber ⛔ interrupted line, amber rail, no chips.
  - A pending confirmation prompt: info rail (not green/red) until you answer.
  - Switch to a non-cyan theme + restart: the info rails + browser/search intent
    chips follow the accent; ok/fail/interrupt stay green/red/amber.
  - Long reply still shows [load more]/[show less] and expands inline.

Code-level proof:
  uv run pytest -q tests/test_syslog_render.py
  -> rail colours (info themed via CYAN token; ok/fail/interrupt the locked hexes);
     chips on JARVIS rows only, NONE on YOU rows; conf chip omitted when conf None;
     NO latency chip ever; intent chip uses design.py maps; interrupt ⛔ + amber
     preserved; [load more]/[show less] expander preserved; 4px rail cell present.
  JARVIS_RUN_QT=1 uv run pytest -q tests/test_syslog_render.py::test_transcript_panel_back_compat_and_sethtml
  -> add_exchange/update_last_jarvis/append_jarvis_scheduled work WITHOUT success
     (default None), rows carry the 7th slot, setHtml accepts the output (qtgui).
  Offscreen width measure (manual): 400px panel -> 360px text, 4px rail renders
  (cyan pixels at x=4-7), no horizontal scrollbar (chip row fits one line).


-----------------------------------------------------------------------
HOTFIX: _TypewriterProxy crashed on the success plumbing (6-arg call)
-----------------------------------------------------------------------
Bug (prod crash): running any command crashed with
  TypeError: _TypewriterProxy.update_last_jarvis() takes from 2 to 5 positional
  arguments but 6 were given   (execution_mixin.py _update_transcript_if_current)
The SYS_LOG redesign added `success` (6th arg) to TranscriptPanel.update_last_jarvis
and add_exchange, but the LIVE call path goes through _TypewriterProxy (the
typewriter wrapper, ui/components/typewriter.py) which has its OWN
update_last_jarvis / add_exchange — those were not updated, so the extra arg blew
up. The pure _build_log_html tests never exercised the proxy → 461 green missed it.

Fix: _TypewriterProxy.update_last_jarvis and add_exchange now accept + forward
success (and _pending carries it through the typewriter ticks). append_jarvis_scheduled
/ update_last_you are NOT overridden on the proxy — they fall through __getattr__ to
the panel (already correct). Full end-to-end arg-count audit done: every def + call
of the four methods matches (core/brain.py + tests use the unrelated
ConversationMemory.add_exchange).

Manual test (uv run python main.py):
  Speak/type any command (e.g. "what's the time") → JARVIS replies and the line
  renders in the SYS_LOG with NO crash (this was the prod traceback). The reply
  types in, then shows its rail + intent/conf chips.

Code-level proof (runs in the DEFAULT suite — closes the proxy coverage gap):
  uv run pytest -q tests/test_syslog_render.py::test_typewriter_proxy_forwards_success_through_to_panel
  -> drives the REAL _TypewriterProxy with the exact 5-positional-arg crash
     signature; asserts success reaches the (stub) panel through the typewriter
     ticks, through the instant add_exchange path, and through the __getattr__
     fall-through (append_jarvis_scheduled). No QApplication (manual _tick) so it
     can't leak Qt singleton state into later tests.


-----------------------------------------------------------------------
Theme swatches show their OWN palette colour (not the active accent)
-----------------------------------------------------------------------
Bug: in CONFIG → HUD THEME, the STARK swatch rendered in the ACTIVE accent (e.g.
green under matrix) instead of cyan. Root cause: the swatch source was already the
per-palette literal, but the fill was applied via setStyleSheet ("background:#00e5ff"),
and the global apply_accent hook (installed under a non-cyan theme) rewrote that
#00e5ff to the active accent. Only STARK matched the base-cyan literal, so only it
was wrong.

Fix (ui/settings.py): the swatch preview bars are now a PAINTED _SwatchBar widget
(QColor via paintEvent, NOT setStyleSheet) sourced from the canonical
_THEME_PALETTES[key]["accent"] — immune to the accent hook. So every swatch fill is
fixed to its own palette. The active-selection indicator (the container border/glow)
is left on the QSS path and MAY follow the active theme — that's the intended "active"
signal.

Manual test (uv run python main.py, on a non-cyan theme e.g. matrix):
  Open CONFIG → HUD THEME. PASS: all five swatches show their OWN colours —
  STARK=cyan, TEAL=teal, AMBER=amber, INDIGO=indigo, MATRIX=green — regardless of
  the active theme. The selected tile's border/glow may be the active accent.

Code-level proof:
  JARVIS_RUN_QT=1 uv run pytest -q tests/test_settings_knobs.py::test_theme_swatch_fill_is_own_palette_not_active_accent
  -> every swatch paints a _SwatchBar whose accent colour == _THEME_PALETTES[key]
     ["accent"] (not the active token). [qtgui — needs a QApplication]
  Amber pixel smoke (manual): with config.theme=amber + install_theme(), the STARK
  swatch's accent bar renders #00e5ff (cyan), proving hook-immunity.


-----------------------------------------------------------------------
Silence the websockets 'keepalive ping failed' traceback on quit
-----------------------------------------------------------------------
Not a functional bug — the app quit cleanly ("Catch you soon" played, read loop
ended, prompt returned). On shutdown we tear down the persistent Deepgram socket;
the websockets LIBRARY runs its OWN internal keepalive thread (separate from our
_keepalive_loop) which, on its next ping after the socket closed, logs a full
ConnectionClosedError traceback (websockets/sync/connection.py:791,
logger.error("keepalive ping failed", exc_info=True)). It's swallowed (not
re-raised) but printed to stderr — looks alarming on every quit.

Fix (core/stt/deepgram.py): a logging.Filter (_SuppressKeepalivePingFailed)
installed on the "websockets" / "websockets.client" loggers at import drops ONLY
that record; every other websockets log still passes. The keepalive thread only
exists because the library sets ping_interval; the SDK's transport shim controls
that and overriding it is fragile, so filtering the benign record is the surgical
fix. (Our [stt] layer already reports the connection errors we actually act on.)

Manual test (uv run python main.py, stt_persistent on):
  Say "okay bye" (or otherwise quit). PASS: JARVIS says goodbye and the app exits
  with NO "keepalive ping failed" traceback in the console. (The one-line
  "[stt] Deepgram read loop ended" is normal teardown logging.)

Code-level proof:
  uv run pytest -q tests/test_stt_streaming.py::test_keepalive_ping_failed_log_is_suppressed
  -> the filter drops a "keepalive ping failed" record, keeps other websockets
     records, and is installed on both websockets loggers.


-----------------------------------------------------------------------
STT resilience on a laggy network (1011 deaths, dead-socket reuse, flood)
-----------------------------------------------------------------------
Symptoms from a flaky-network run: a persistent Deepgram socket died mid-utterance
with "1011 (internal error) keepalive ping timeout" (only "Could you please tell me
the" was captured), then the DEAD warm socket was reused next turn ("no speech
within timeout"), and the console flooded with dozens of identical "Deepgram send
failed" lines. Three fixes:

1) Disable the websockets LIBRARY ping (core/stt/deepgram.py _pingless_ws_factory +
   DeepgramClient(transport_factory=...)). On a laggy network the library self-closes
   the socket with 1011 when a pong is slow; we run our OWN app-level KeepAlive
   (send_keep_alive every 5 s) so Deepgram won't idle-close us, making the library
   ping pure downside. The factory returns the SAME websockets ClientConnection the
   SDK would build (identical .send/.recv/iterate contract) with ping_interval=None
   + open_timeout=8. Defensive: if the SDK build doesn't accept transport_factory
   (TypeError), fall back to defaults (ping on) — streaming still works.

2) Abandon a broken socket instead of flooding (deepgram.py _locked_send). On the
   first send failure we now set _stop, so the writer/keepalive/reader wind down and
   every subsequent send short-circuits at the guard — the failure is logged ONCE,
   not once per queued frame + keepalive tick. is_alive() then returns False.

3) Never reuse a dead warm socket (core/voice.py _run_streaming_stt). After each
   persistent turn, if session.is_alive() is False we _discard_persistent(), so the
   NEXT turn opens a fresh socket instead of retrying a corpse (which caused the "no
   speech" stall).

Manual test (uv run python main.py, stt_persistent on):
  Use it over several turns (ideally on a slightly flaky connection). PASS: if a
  socket dies, you see at most ONE "send failed" line (not a flood), the next turn
  opens a fresh "Deepgram persistent socket open" instead of stalling on "no speech",
  and you should no longer see "1011 keepalive ping timeout" closing the socket
  mid-utterance. If your network is very unstable, stt_persistent=false (fresh socket
  per turn) is still the most robust.

Code-level proof:
  uv run pytest -q tests/test_stt_streaming.py
  -> test_send_failure_abandons_socket_and_stops_flood: a failing send sets _stop,
     is_alive() -> False, the error is surfaced once, and further sends short-circuit
     without hitting the socket (no flood).
  (The transport_factory / ping-disable is exercised live — it needs a real Deepgram
  socket; the factory is contract-identical to the SDK default apart from ping.)


-----------------------------------------------------------------------
Decouple confidence from outcome (no more 0% on a failed run)
-----------------------------------------------------------------------
Bug (semantics): when a command/step failed, the HUD zeroed the confidence to 0%
(execution_mixin.py: `hud_conf = 0.0 if not exec_ok else conf`). That conflates two
different axes — "did JARVIS understand you" (routing confidence) vs "did the action
succeed". A github.com page that loaded too slowly, or a 3-step workflow where 2/3
steps ran, would show 0% even though JARVIS understood perfectly (97%). 0% reads as
"nothing worked".

Fix: confidence ALWAYS shows the real routing confidence; success/failure is carried
ONLY by the dedicated outcome channels — the sys-log colour rail (red), the gauge's
FAIL label, the red error toast, the voice page "err" status. So a failed run reads
"97% — FAIL" (understood you, but it failed), never "0%". Applies to ALL intents.
- `hud_conf = conf` (no longer zeroed) → feeds the gauge %, the sys-log conf chip,
  history conf, last-action, and the voice page with the REAL value.
- The gauge band/FAIL label is computed by a new pure helper
  `_ExecutionMixin._confidence_display(conf, exec_ok)` → (pct, label): pct is the
  real %; label is "FAIL" on failure else HIGH/MED/LOW.
Audit confirmed NO code infers failure from conf==0: the gauge FAIL reads exec_ok,
the sys-log rail reads success, history reads status=="error", the voice row reads
its own is_fail. Decoupling is safe.

Manual test (uv run python main.py):
  Run a command that FAILS — e.g. a browser step whose page is very slow (times out),
  or any failing action. PASS: the right-panel gauge shows the REAL confidence
  (e.g. 97%) with a FAIL label — NOT 0%; the sys-log row is a red rail with the real
  % in its conf chip; the toast is the red error. A SUCCESSFUL command is unchanged
  (real % + HIGH/MED/LOW + green). A 3-step workflow where the last step fails still
  shows the real routing confidence, not 0%.

Code-level proof:
  uv run pytest -q tests/test_workflow_offthread.py::test_confidence_display_never_zeroed_on_failure
  -> _confidence_display(0.97, False) == (97, "FAIL")  (was the bug: (0, "FAIL"));
     success still maps to HIGH/MED/LOW; bounds clamped.


-----------------------------------------------------------------------
Navigation: salvage a slow-but-loaded page (no more false FAIL on slow nav)
-----------------------------------------------------------------------
Bug: a navigation to a heavy/slow page (e.g. github.com on a laggy connection)
was marked FAIL when it took >15 s to reach DOMContentLoaded — even though the
page DID load moments later. The success bar was already the lenient
"domcontentloaded" (NOT full "load"), so the real causes were (a) a 15 s ceiling
too short for a slow network and (b) a hard-fail on timeout with no salvage.

Fix (core/browser/interaction.py + config/settings.py):
1) Configurable timeout — new config.browser_nav_timeout_ms (default 30000),
   read at CALL TIME via _nav_timeout() (Settings change applies without restart),
   shared by navigate / go_back / go_forward / refresh / hard_refresh. Locator ops
   (clicks, inner_text) keep their own shorter 15 s _TIMEOUT.
2) Salvage on timeout — _salvage_after_timeout(url): when goto() times out, DON'T
   blind-fail. Return success ONLY when ALL hold: the navigation committed to the
   TARGET host (registrable-domain match, so a www. redirect is fine but an off-host
   redirect is not), document.readyState is "interactive"/"complete", AND there's
   real content (a non-empty <title> OR body text). Logs a distinct
   "✓ loaded slowly (salvaged after timeout)". Any check raising/uncertain → fail
   (conservative). NOTE: no HTTP status is available after a goto timeout, so a
   slow-but-rendered 4xx from the right host counts as "loaded" — an accepted,
   vanishingly-rare edge (a >30 s page that's ALSO an error page); deliberately not
   guarded (no status listener).

Scope: navigate() is the single path for "open <site>", search_web, open_url, AND
workflow navigate steps — one fix covers all. (Pairs with the earlier confidence
decouple: a salvaged-slow nav is now a real success, and even a true failure shows
the real routing % not 0%.)

Manual test (uv run python main.py):
  Navigate to a heavy/slow site (or throttle your connection). PASS: if the page
  loads even after the old 15 s, JARVIS reports SUCCESS — you'll see
  "✓ loaded slowly (salvaged after timeout)" in the log — instead of a false
  "Page took too long". A genuinely dead/blank URL still fails (after the 30 s
  ceiling). Tune config.browser_nav_timeout_ms in config/jarvis.json if needed.

Code-level proof:
  uv run pytest -q tests/test_browser_navigate.py
  -> clean load = success (unchanged); timeout+committed+interactive+title = SUCCESS
     (salvage); timeout+complete+body-only = SUCCESS; www-redirect same domain =
     SUCCESS; timeout+blank = FAIL; timeout+readyState=loading = FAIL; timeout+wrong
     host = FAIL; _nav_timeout() reads config (+ default on garbage); navigate passes
     the configured timeout to goto().


-----------------------------------------------------------------------
§28 — Confidence no longer zeroed on a FAILED scheduled reminder-action
-----------------------------------------------------------------------
Background: §26 decoupled confidence from outcome on the main command path
(_finish_execute now shows the REAL routing % even on a failure, never 0%). But
its audit missed a sibling: _on_reminder_action (the delayed-reminder-WITH-an-
executable-step slot in backend_signals_mixin.py) still re-implemented the conf
logic inline as `hud_conf = sc if exec_ok else 0.0`, so a failed scheduled action
read 0% on the LAST ACTION chip, the Voice page, and the persisted history. Fix:
`hud_conf = sc` (mirrors the main path's `hud_conf = conf`) — the divergent inline
re-implementation is removed so it can't drift again. The sys-log rail was already
correct (success=exec_ok) and is untouched.

This path needs a reminder that carries a `run` step (an executable scheduled
action) AND a failure to observe by hand, so it's proven by the automated test.

Manual sense-check (optional): schedule a reminder whose action will fail, let it
fire, and confirm the HUD's confidence reads the real % with a FAIL/err indicator
— NOT 0%.

Code-level proof (no timers, no disk):
  uv run pytest -q tests/test_reminder_scheduled.py
  -> test_reminder_action_failure_preserves_real_confidence: a FAILED scheduled
     action keeps the real schedule % (0.92) in history, the LAST ACTION chip, and
     the Voice page, while success=False still drives the red sys-log rail.
     test_reminder_action_success_reports_confidence: the success path is unchanged.


-----------------------------------------------------------------------
§29 — Slow-but-loaded go_back / go_forward / refresh now salvage too
-----------------------------------------------------------------------
Background: §27 added _salvage_after_timeout so a navigate() to a slow page that
DID load is a SUCCESS, not a false FAIL. But the sibling nav methods (go_back /
go_forward / refresh) still hard-failed on a slow load — the same bug, left in the
siblings. They now REUSE the same salvage helper.

The salvage's commit-check adapts to what's known:
  - navigate / refresh have an explicit target URL → host-match it (a www. redirect
    is fine; an off-host redirect is a conservative FAIL). refresh host-matches the
    pre-reload URL (a reload that redirected to another host fails).
  - go_back / go_forward have NO destination URL to match (it's whatever history
    entry they land on), so instead they require the URL actually MOVED off the
    pre-nav page — a salvage can't pass for a history nav that never committed —
    plus the same readyState (interactive/complete) + real-content (title or body)
    guards. (hard_refresh is unchanged: it was already lenient — it swallows the
    load-state wait timeout — so it never false-failed.)

Manual test (uv run python main.py, on a slow/throttled link):
  1. Navigate somewhere, then "go back" / "go forward" / "reload the page" on a
     heavy page that takes >30 s to hit DOMContentLoaded but DOES render.
     PASS: JARVIS reports SUCCESS with "loaded slowly (salvaged after timeout)" in
     the log, instead of "go_back/refresh timed out".
  2. A genuinely dead reload / a back that doesn't commit still FAILS (after the
     30 s ceiling) — the salvage is conservative.

Code-level proof:
  uv run pytest -q tests/test_browser_navigate.py
  -> go_back: clean=success; no-history=fail; timeout+committed+title=SUCCESS;
     timeout+committed+body-only=SUCCESS; timeout+NOT-committed=FAIL (moved-off
     guard); timeout+blank=FAIL; timeout+loading=FAIL. go_forward:
     committed=SUCCESS, not-committed=FAIL. refresh: clean=success; same-host
     timeout=SUCCESS; off-host-redirect=FAIL; blank=FAIL. (navigate matrix from §27
     unchanged.)


-----------------------------------------------------------------------
§30 — Failure-narration rail + honest _reg_domain docstring (audit #2/#3)
-----------------------------------------------------------------------
Two small audit fixes that ride together:

#3 (cosmetic, sys-log rail) — the post-execution narration line (on a FAILURE, the
"couldn't reach X — try Y" explanation spoken after the primary reply) was appended
WITHOUT a success arg, so it defaulted to a neutral/info (cyan) rail even when the
action failed — while the primary line above it correctly showed the red FAIL rail.
The action's real outcome is now threaded through the _action_followup_tts signal
(5→6 args: +success) so the explanatory line gets the SAME rail as the primary line:
red on failure, green on a success suggestion.

  Manual check: cause an action to fail (e.g. navigate to a dead domain) so JARVIS
  speaks a follow-up suggestion. PASS: both the primary reply line AND the follow-up
  suggestion line under it show the RED outcome rail (previously the follow-up was
  cyan/info). A successful action's continuation suggestion shows the GREEN rail.

#2 (docstring only, no behaviour change) — _reg_domain (core/browser/interaction.py)
is a plain LAST-TWO-LABELS heuristic, NOT a true registrable domain; it's wrong for
multi-label public suffixes (bbc.co.uk and example.co.uk both collapse to co.uk). The
docstrings (the helper + the salvage that calls it) used to imply "registrable-domain"
precision; they now state honestly what it does and note the bounded failure mode (a
timed-out nav that also redirected to a different second-level site under the same
multi-label TLD could be salvaged — vanishingly rare). No public-suffix dependency
added by design.

Code-level proof:
  uv run pytest -q tests/test_workflow_offthread.py
  -> test_followup_failure_line_gets_fail_rail (success=False reaches the rail),
     test_followup_success_line_gets_ok_rail (success=True), and
     test_followup_default_success_is_backcompat (the slot keeps a default so an
     un-migrated 5-arg emit still renders). #2 is docstring-only — no test needed
     (the salvage host-match behaviour is already covered in §27/§29's matrix).


-----------------------------------------------------------------------
§31 — SYS_LOG no longer writes the reply twice (narration/primary race)
-----------------------------------------------------------------------
Background: the SYS_LOG showed a command's reply TWICE — row 1 stuck on the
"Computing." thinking placeholder (empty [] timestamp + UNKNOWN chip), and a
SECOND JARVIS-only row with the real reply (BROWSER 97%). Seen on a failing
command (dead URL → ERR_NAME_NOT_RESOLVED).

Root cause (NOT an empty primary — the primary text is correct): the
post-execution narration daemon ("name the cause, suggest a next step") runs
CONCURRENTLY with the primary TTS and appended its transcript row WITHOUT waiting
for the primary. When the narration's HTTP won the race against the primary's TTS
on_ready, it took the rows[-1] slot first; the primary's on_ready update then
landed on the narration row instead of replacing "Computing." on the command row —
so the command row was orphaned and the reply looked written twice.

Fix (Option A — sequencing, NOT row-id anchoring): the narration is now BUFFERED
and only appended AFTER the primary for that command finishes (its on_done), which
is the SAME sequencing the responder data-follow line already uses. The narration
HTTP still fires early (its latency overlaps TTS); only the APPEND waits. A fallback
timer (12 s) force-flushes the narration if the primary never signals done (e.g. the
say() exception path), so it's never lost. The muted/skip-TTS paths fire on_done
synchronously, so they flush immediately — unchanged.

Scope: this covers EVERY narration-triggering command, not just failed browser navs
— every failed action-intent, every run_workflow, and suggest-on-success — because
the sequencing is at the narration-append site (general by construction).

PHYSICAL TEST (a race fix can't be trusted on ONE green run — repeat it):
  1. Keep AUTO_CONFIRM as-is. Run a FAILING command that triggers narration, e.g.:
        "open the website http://vlaneine12345.com"   (a dead/unresolvable URL)
     Do this 5-6 TIMES IN A ROW (same or different dead URLs).
  PASS (fixed): EVERY time you get ONE clean primary row on the command line
     (real timestamp, BROWSER + conf chips, red outcome rail) and the spoken
     narration appears as its OWN row AFTER it. NEVER a split where row 1 is stuck
     on "Computing." with an empty [] timestamp / UNKNOWN chip and the reply shows
     on a second row.
  FAIL (old): intermittently (it's a race) row 1 froze on "Computing." [] UNKNOWN
     and the reply was written again as a second row.
  Also exercise the other narration paths a couple of times each:
     - a workflow: "open chrome and take a screenshot"  (run_workflow always narrates)
     - a success suggestion: "open chrome"               (suggest-on-success)
  PASS: single clean primary row + a later narration/continuation row, every time.

Code-level proof (forces the narration-before-primary ordering — no hoping):
  uv run pytest -q tests/test_workflow_offthread.py
  -> test_narration_before_primary_is_buffered_then_flushed: narration arriving
     before the primary is BUFFERED (no row), the primary lands on the COMMAND row
     (replaces 'Computing.', real ts + chips), then the narration flushes as its own
     row after — and no orphaned 'Computing.' row survives.
     test_narration_after_primary_appends_immediately (normal non-racing path),
     test_narration_fallback_flushes_when_primary_never_signals (never lost),
     test_narration_dropped_when_command_superseded (stale narration not mis-appended).


-----------------------------------------------------------------------
§32 — SYS_LOG row-id anchoring (replaces §31's narration buffering)
-----------------------------------------------------------------------
Background: §31 (Option A) buffered the narration to append after the primary's TTS
on_done. It cut the first split but the bug MUTATED: TTS on_done is not synced to the
typewriter, so the narration could append while the primary reply was still typing
char-by-char. The proxy typewriter targeted "the last row" (rows[-1]), so its
remaining ticks crossed onto the just-appended narration row — leaving the command
row frozen at PARTIAL text with an UNKNOWN chip and the reply (full text + BROWSER %)
on a second row. Both manifestations shared one root cause: rows[-1] targeting with a
concurrent appender.

Fix (Option B — structural): every transcript row gets a stable id at creation. The
typewriter captures the id of the row it's animating (you/thinking/jarvis) and every
tick targets THAT id via panel.update_jarvis/update_you — never rows[-1]. A narration/
follow/reminder row appends with its OWN id and never disturbs the in-flight reply. So
ordering and on_done/typewriter timing are irrelevant by construction. §31's buffering
(_pending_narration, _primary_done_token, the 12s fallback, the on_action_followup_tts
gate) was REMOVED as dead complexity; the success-rail threading (§30) was kept. Audio
stays ordered because TtsEngine has a single worker queue and the primary say() is
enqueued before the narration thread starts.

PHYSICAL TEST — a race/ordering fix can't be trusted on ONE run. Repeat it:
  1. Run a FAILING command that triggers narration, e.g.:
        "open the website http://vlaneine12345.com"   (dead/unresolvable URL)
     Do this 5-6 TIMES IN A ROW.
  PASS (fixed): EVERY time — ONE clean command row (YOU + the reply, real timestamp,
     BROWSER + conf chips, red rail) and the spoken narration as its OWN row AFTER it.
     NEVER a split: no row frozen on "Pondering…"/"Computing…", no row stuck at PARTIAL
     reply text with an UNKNOWN chip, no second copy of the reply.
  FAIL (old, either manifestation): row 1 frozen on the thinking word OR on a partial
     of the reply with UNKNOWN, and the reply written again on a second row.
  2. Also a couple of times each (other narration paths — same fix covers them):
        - workflow: "open chrome and take a screenshot"   (run_workflow narrates)
        - suggest-on-success: "open chrome"                 (continuation line)
  PASS: single clean command row + a later narration/continuation row, every time.
  3. Sanity: a long reply that types visibly (e.g. "read my screen") with nothing else
     firing — types smoothly to completion on its own row, correct chip at the end.

Code-level proof (forces the bad ordering — drives _tick manually, appends mid-anim):
  uv run pytest -q tests/test_syslog_render.py
  -> test_proxy_append_mid_typewriter_keeps_reply_on_its_own_row: append a narration
     row WHILE the reply is mid-typewriter; the full reply + final chip still land on
     the COMMAND row's id, narration stays its own row, no orphan.
     test_proxy_narration_before_primary_lands_on_command_row: narration appended
     before the primary starts → primary still lands on the command row.
     test_typewriter_proxy_forwards_success_to_its_own_row: success reaches the right
     row by id. (qtgui) test_panel_by_id_update_targets_correct_row_after_append:
     update_jarvis(id) edits its row after another appended; update_last_* back-compat.


-----------------------------------------------------------------------
§33 — Narration (2nd response) types out AFTER the primary (sequential)
-----------------------------------------------------------------------
Background: after §32 (row-id anchoring) the narration/follow line stopped animating
— it popped in instantly while the primary was still typing. (It only LOOKED animated
before, as an artifact of the old cross-row bug.) Now the narration TYPES OUT on its
own row, sequenced to start only after the primary finishes — mirroring the audio,
which already plays primary-then-narration via the single TTS queue.

How: the proxy's append_jarvis_scheduled gained an `animate` flag. The narration/follow
path passes animate=True → the row is reserved (empty, invisible) immediately for
correct order, and its type-out is QUEUED to run after the primary's animation
completes (one animation at a time). Reminders + scheduled-workflow lines keep the
default animate=False → they append INSTANTLY and never enter the queue (async
notifications must appear at once and can't collide with a command's in-flight reply).
Option B holds throughout: each animation writes only to its own captured row id.

PHYSICAL TEST (repeat a few times each):
  1. Failing command with a narration, e.g. "open the website http://vlaneine12345.com":
     PASS: the primary reply ("Couldn't load…") types out fully on the command row,
     THEN the spoken follow-up types out on its OWN row below it. Never both at once,
     never an instant pop-in of the second line, never a split/partial-UNKNOWN orphan.
  2. Success with a continuation, e.g. "open chrome":
     PASS: the reply types out, then the suggestion line types out after it.
  3. Esc mid-reply (while the primary is typing): PASS: clean abort, the row shows
     Interrupted, and the queued narration does NOT ghost-type in afterward.
  4. Reminder mid-command: set a 5-second reminder, then issue a command; when the
     reminder fires PASS: it POPS IN instantly (not typed out) and does not disturb the
     command's in-flight typing.

Code-level proof (deterministic — drives _tick manually, forces narration-early):
  uv run pytest -q tests/test_syslog_render.py
  -> test_sequential_animation_resp1_then_resp2: resp 2 appended mid-resp-1 is RESERVED
     + queued (not animating); resp 1 finishes on its row; THEN resp 2 animates on its
     own row — neither crosses.
     test_narration_during_thinking_waits_for_primary (queued through thinking),
     test_no_narration_primary_animates_alone, test_interrupt_clears_pending_animation_
     queue (no ghost-type), test_reminder_append_stays_instant_and_unqueued (animate=
     False isolation), test_new_command_finalizes_pending_animations (snap-to-full).


-----------------------------------------------------------------------
§34 — create_file no longer writes the redacted "<N chars>" placeholder
-----------------------------------------------------------------------
Background: asking JARVIS to write a file (esp. the 2nd+ create_file in a session)
could land the literal text "<4921 chars>" in the file instead of the real content.
Root cause: privacy redaction (R3-13) stores dictated content/text/value/code in
conversation memory as "<N chars>". That history is replayed to the model, and the
model COPIES the placeholder into a new create_file's content → it gets written to
disk. Data loss. (The redaction protects disk/privacy but poisoned the model's replay.)

Fix — two layers:
  Layer 1 (root cause): memory.get_prompt_messages() — the MODEL-FACING replay — drops
    any redacted "<N chars>" param from assistant turns (the model sees the action +
    path but no content to copy, so it generates real content). The stored/persisted
    form still keeps "<N chars>" (privacy/R3-13 unchanged). brain.ask_claude uses
    get_prompt_messages() for both the primary call and the JSON-retry path.
  Layer 2 (backstop): create_file / append_file / replace_in_file refuse content that
    is EXACTLY a "<N chars>" placeholder (re.fullmatch), so it can never reach disk
    even if the model emits it. Returns a clear error asking the user to re-issue.

PHYSICAL TEST — the key one is TWO create_file commands in ONE session:
  1. "create a python file in tests/ that prints hello world"  -> confirm -> open the
     file. PASS: it has REAL code, not "<N chars>".
  2. WITHOUT restarting, ask for a SECOND, bigger file, e.g. "write a python file in
     tests/ that builds a small CLI calculator"  -> confirm -> open it.
     PASS (fixed): the SECOND file also has REAL content. (This is the case that used
     to fail — the first create_file entered memory as "<N chars>" and the model
     copied it into the second.)
  3. Backstop sanity (optional): if a "<N chars>" ever slips through, the file is NOT
     written — JARVIS says it looks like a redacted placeholder, re-issue. Nothing
     lands on disk.
  Cleanup: delete the test files you created. (Note: tests/file_search_engine.py is the
  old corrupted artifact from this bug — it contains just "<4921 chars>"; safe to delete.)

Code-level proof:
  uv run pytest -q tests/test_memory_redaction.py tests/test_file_ops_package.py
  -> Layer 1: get_prompt_messages drops the redacted content key while get_messages/
     disk keep "<N chars>"; handles the inject_outcome "[Result: …]" suffix; fail-soft
     on non-JSON; non-secret params untouched. Existing R3-13 redaction tests still
     green (disk format unchanged).
     Layer 2: _is_redacted_placeholder exact-match; create/append/replace refuse
     "<N chars>" (nothing written) while normal content still writes.


-----------------------------------------------------------------------
§35 — large generations no longer time out at the brain (15s ceiling)
-----------------------------------------------------------------------
Background: asking JARVIS to generate a big file/script (e.g. the ~5k-char "Mini File
Search Engine" challenge) returned "I couldn't reach my brain right now — try that
again." with a clean red/UNKNOWN row. The brain WAS reachable — the model was still
GENERATING when a flat 15s timeout fired and cut it off. Small commands (2-4s) were
fine; only large-output requests blew the ceiling.
Root cause: the brain call (core/brain.py) is NON-streaming, so the whole response
arrives only after the model finishes — no bytes flow during generation, so httpx's
READ timeout behaves like a total generation deadline. timeout=15 (a bare float) set
connect/read/write/pool ALL to 15s, and the SDK retried the timeout twice → a doomed
big generation re-ran 3× (~45s) before surfacing api_timeout.
Fix: _api_timeout_for(max_out) builds a SPLIT httpx.Timeout — connect/write/pool stay
~10s (a dead network still fast-fails), READ scales off the SAME max_out the brain
already computes via _infer_max_output_tokens (2048->20s, 8192->60s, 16384->90s). The
big-content path (max_out > 2048) also caps SDK retries to 0 (no 3× regeneration); fast
routing calls keep the default retries. Applied at BOTH the primary call and the
529-overload retry block.

PHYSICAL TEST — the exact repro:
  1. Ask for a large file, e.g. "create a python file mini_search.py in tests/ that
     implements a mini file search engine: recursive indexing, ranking, and a CLI"
     -> confirm.
     PASS (fixed): it COMPLETES and writes real code (used to fail with "couldn't
     reach my brain" after ~45s).
  2. A normal quick command still snaps back fast: "what time is it" -> instant.
  3. (Negative, optional) Pull your network cable / disable Wi-Fi, then issue any
     command. PASS: it still fails FAST (~10s connect), not a 90s hang — the long
     budget is on READ only, not connect.
  Cleanup: delete tests/mini_search.py if created.

Code-level proof:
  uv run pytest -q tests/test_brain_timeout.py
  -> read scales with budget (20/60/90s); connect/write/pool stay 10s at every budget;
     big-prompt path gets read=90s AND caps retries (with_options(max_retries=0)); short
     command keeps read=20s and does NOT cap retries; APITimeoutError still surfaces the
     api_timeout fallback; a 529 mid-generation retries with the SAME scaled timeout at
     both call sites (never falls back to the old 15s).


-----------------------------------------------------------------------
§36 — docx/doc generator failures are now diagnosable (Fix A)
-----------------------------------------------------------------------
Background: a Word-doc creation failed with only "✗ Script ran but the output file is
missing. [ERR 1]". The generated python-docx script was PROVEN good (runs standalone,
produces a valid file). The failure is in the handler's run-and-verify, and it was
UNDIAGNOSABLE because the generator subprocess's stdout/stderr was discarded — only a
5-line tail reached the user on a non-zero exit, and NOTHING at all on the rc==0
"output file is missing" branch. ("[ERR 1]" is just _fail()'s generic terminal_done(1)
— NOT the script's exit code.) Extra clue: docx worked on a different PC and broke after
the project was transferred to this machine (~Apr 2026) — so a machine-specific path
(OneDrive-redirected Documents, stale username, baked path) is in play.
Fix A (diagnostics only — no behaviour change to the happy path): on EVERY failure
branch (timeout / non-zero exit / missing-file) the handler now logs to the debug log:
  - the exit code, target_path, its resolved form, and whether it exists;
  - the path the SCRIPT said it saved to (parsed from the script's "OK: <path>" line)
    and whether THAT exists / matches target_path — the key path-divergence signal;
  - machine context: OneDrive redirect (under_onedrive + $OneDrive), home, tmp cwd,
    and which python (sys.executable) ran the script;
  - the FULL subprocess stdout+stderr (not a 5-line tail).
The user-facing message is also richer: the missing-file case now says whether the
script saved to a DIFFERENT path than expected, names both paths, and shows a 12-line
tail, ending with "(full subprocess output in debug log)".

PHYSICAL TEST (this surfaces the REAL cause of the original failure):
  1. Ensure debug_mode is ON (config/jarvis.json "debug_mode": true) so [doc] debug
     lines print.
  2. Re-run the failing request: "create a word document in my tests/ folder: a
     detailed 2-page report on the history of artificial intelligence".
  3. If it FAILS again, read the terminal/debug log for the "--- generator diagnostics
     (missing-file) ---" block. PASS: you can now see the script's "OK:/ERR:" line, the
     actual saved path, the OneDrive flag, and the full traceback — enough to tell
     whether Sonnet echoed a wrong path OR an environment path issue. (This is the
     evidence that decides Fix B's exact form.)
  4. If it SUCCEEDS, the happy path is unchanged — the file lands in tests/ as before.

Code-level proof:
  uv run pytest -q tests/test_document_diagnostics.py
  -> _script_reported_path pulls the script's "OK: <path>" (last one wins; None when
     absent); _log_run_diagnostics returns the reported path, logs machine context
     (OneDrive, python) and the FULL output (traceback line survives — not truncated),
     and handles the no-output case.


-----------------------------------------------------------------------
§37 — doc-generator validator calibrated (the REAL cause of §36) (Fix B)
-----------------------------------------------------------------------
Background: Fix A (§36) surfaced the true cause of the docx failures — the AST validator
(_validate_script) HARD-BLOCKED the generated script over an innocent line:
  for attr in ('left_margin','right_margin','top_margin','bottom_margin'):
      setattr(section, attr, Inches(1))
"✗ Script blocked — unsafe call detected: call to setattr()". The script never ran → no
file anywhere. It was intermittent ("worked on the old PC") because Sonnet's output
varies — sometimes setattr, sometimes not. It was never the machine.
Root cause = a CLASS bug: the validator blocked any call whose bare name OR attribute
name matched one deny-set, receiver-blind. Proven siblings (same trap): getattr(...) and
— worse — re.compile(...) (attr "compile"), even though `re` is allowlisted so Sonnet
CAN use it. Fix setattr alone and the next re.compile/getattr puts us right back here.
Fix B — three intent-specific tiers, receiver-aware (keeps the sandbox a real safety net):
  - _DANGEROUS_BUILTINS {eval,exec,compile,__import__} — blocked as a bare Name call
    ONLY (so re.compile passes); the rare __builtins__.compile(...) form is still caught.
  - _DANGEROUS_OS_CALLS {system,popen,execv*,fork*} — blocked as Name OR Attribute
    (catches os.system(...) AND `from os import system; system(...)`).
  - getattr/setattr/delattr — ALLOWED on ordinary objects; BLOCKED only when the first
    arg reaches into a guarded powerful module (os/sys/builtins/importlib/subprocess/
    ctypes/socket). `setattr(section,'left_margin',…)` passes; `getattr(os,'system')`
    stays blocked. (Accepted residual, disclosed: an aliased escape like
    `import os as o; getattr(o,'system')` isn't caught — needs an adversarial generator;
    network is proxy-blocked, subprocess runs with no args / temp cwd / memory cap.)
Also: a one-line nudge added to all four skills/*/SKILL.md ("set attributes directly,
never via setattr/getattr") so Sonnet rarely even exercises the guard; and the block
message is now plain-language + a retry hint, streamed to the panel via _fail (Fix A's
diagnostics untouched).

PHYSICAL TEST (the real gate — green tests proved nothing here until Fix A):
  1. "create a word document in my documents folder: a short one-page summary of how
     neural networks work"  -> confirm -> PASS: a real .docx lands in Documents (this
     is the exact doc that failed on the setattr block).
  2. "create a word document in my tests/ folder: a detailed 2-page report on the
     history of artificial intelligence"  -> confirm -> PASS: real .docx in tests/.
  3. Regression sanity: if a generation ever IS blocked, the panel shows a plain-English
     reason ("...the generated script used a pattern the sandbox blocks (...). Try
     again..."), not a bare "[ERR 1]".

Code-level proof:
  uv run pytest -q tests/test_document_validator.py
  -> PASS set: setattr(section,…), the VERBATIM margin loop, getattr(obj,'x',None),
     delattr(obj,…), re.compile, an innocent docx script.
     BLOCK set: eval/exec/compile/__import__ builtins; os.system/os.popen and
     `from os import system; system()`; getattr(os,'system') / setattr(sys,…) /
     delattr(os,…) escapes; __builtins__.compile; import socket/subprocess; a
     non-allowlisted import (pandas).


-----------------------------------------------------------------------
§38 — doc generator: Job Object killed the subprocess at startup (Fix C)
-----------------------------------------------------------------------
Background: after Fix B, docx STILL failed — but with a NEW, cleaner signature that
Fix A's diagnostics surfaced: "Script ran (exit 0) but no file appeared" with
"(no subprocess output captured)". exit 0, ZERO output (not even the script's own
"OK:"/"ERR:" print), no file. Reproduced cold: even a trivial print("OK") script
through _run_generator gave exit 0 + no output + no file; neutering the limiter made
the file appear. ROOT CAUSE: _try_limit_generator_process created a Windows Job Object
with KILL_ON_JOB_CLOSE, assigned the child, then RETURNED — dropping the only reference
to the job handle. The handle GC'd → job's last handle closed → KILL_ON_JOB_CLOSE
killed the child at startup. Dormant until pywin32 was installed on this machine (old
PC had no pywin32 → limiter was a no-op → docs worked). The transfer memory was real,
just for this: pywin32 presence, not the path/OneDrive.
Fix C — three parts:
  1. CORE: stash the handle on the proc (proc._jarvis_job = hjob) so it outlives the
     function. The poll loop holds proc; by the time both GC, the child has exited —
     nothing left to kill. The 512 MB cap AND kill-child-if-JARVIS-dies both stay intact.
  2. DEFENSIVE: the missing-file branch now special-cases exit-0-with-NO-output (the
     exact silent signature) with a LOUD, named message ("killed before it could run …
     set doc_generator_limit_enabled=false …") — never again a silent exit-0.
  3. KILL SWITCH: new config doc_generator_limit_enabled (default ON). Flip to false to
     disable the limiter instantly if it ever misbehaves again.

PHYSICAL TEST (the real verdict — run each a couple of times, Sonnet output varies):
  1. "create a word document in my documents folder: a short one-page summary of how
     neural networks work"  -> confirm -> PASS: a real, OPENABLE .docx in Documents
     (this is the doc that failed with the silent exit-0).
  2. "create a word document in my tests/ folder: a detailed 2-page report on the
     history of artificial intelligence"  -> confirm -> PASS: real .docx in tests/.
  3. Kill-switch sanity (optional): set "doc_generator_limit_enabled": false in
     config/jarvis.json -> docs still generate (limiter skipped entirely).

Code-level proof:
  uv run pytest -q tests/test_document_sandbox.py
  -> config flag defaults ON; and (win32 + pywin32 guarded) a real docx script through
     _run_generator with the limiter ENABLED now PRODUCES an openable file — this test
     would have FAILED before Fix C (child killed at startup).


-----------------------------------------------------------------------
§40 — create_file no longer silently makes a 0-byte file (A: spec, B: net)
-----------------------------------------------------------------------
Background (found in the pre-sleep sweep): "create a python file in tests/ that prints
fizzbuzz" -> the BRAIN emitted create_file with `path` but NO `content` -> a 0-byte EMPTY
file; user had to ask a 2nd time before code appeared. NOT phrasing-determined (identical
"...that prints X" wording gave both empty and full) - the model was free to omit content
because the CLAUDE.md create_file spec was silent on it; an in-conversation correction
flipped it. Fast-path (workflow_nlu.py) and executor were both exonerated - brain spec gap.
Fix - two parts:
  A (root cause): CLAUDE.md create_file rule - `content` is REQUIRED whenever the user
    DESCRIBES what the file should contain/do ("a python file that prints fizzbuzz", "a
    CLI todo app with SQLite"); generate the full body in the SAME call, never an empty
    file + follow-up. Omit content ONLY for an explicit blank file ("create an empty file
    log.txt"). + a worked example.
  B (visibility net - because auto_confirm/workflow can SKIP the card): an empty create is
    never silent regardless of confirm path. Layer 1: the confirmation card shows "Note:
    this will create an EMPTY file (no content)." Layer 2: the post-write closure emits a
    warn "created EMPTY - X (0 B)" line + an info toast (signals.notice) on a 0-byte write,
    instead of a plain check that reads as success. Intentional empties are FLAGGED, never
    blocked.

PHYSICAL TEST (the real gate - model behaviour isn't deterministically unit-testable, so
test COLD: fresh session, no correction priming the history):
  1. "create a python file in tests/ that prints fizzbuzz" -> PASS: code is in the file on
     the FIRST ask (no empty file, no asking twice). Open it to confirm real code.
  2. "write a python file in tests/: a full CLI todo app with SQLite, add/list/done/delete"
     -> PASS: full code on first ask.
  3. Regression - intentional empties preserved: "create an empty file blank.txt in tests"
     -> still 0 bytes (allowed), and the card shows the EMPTY note + a 0-bytes heads-up.
  4. "create a file log.txt in tests" (bare, no described content) -> still empty via the
     deterministic fast-path (unchanged).

Code-level proof (Part B is deterministic; Part A is prompt-driven -> physical only):
  uv run pytest -q tests/test_file_ops_package.py tests/test_file_creation_routing.py
  -> empty create flags the card ("EMPTY file" in the prompt) AND emits the "created
     EMPTY" post-write line (not a plain check), while still WRITING the 0-byte file
     (intentional empties allowed); a real-content create shows no flag and writes the
     content; the fast-path routing tests stay green (workflow_nlu.py untouched).

-----------------------------------------------------------------------
§41 — close_all_engines: executor enumerates live engines (no brain guess)
-----------------------------------------------------------------------
Background (sweep find #2): "close the two browsers you just opened" (Chrome+Firefox
live) -> the BRAIN guessed [close firefox, close edge], closed firefox OK, then FAILED
on "edge isn't open" (edge was never open). The executor tracks the live set perfectly
(_SessionBase._sessions); the brain had zero visibility and guessed a default pair.
Fix (Option A): new browser_automation action `close_all_engines` — the executor
enumerates self._sessions at run time and closes them all (no naming, no counting),
immune to a brain miscount. Plural/all/quantified phrasing routes here; a SINGULAR
"close the browser" still routes to close_engine (active engine). Friendly no-op (not an
error) when none open. Deferred: injecting open-engine state into brain context (Option
B) — only needed for "close some-but-not-all named" subsets, not paid for now.

PHYSICAL TEST (routing is model-driven -> verify live):
  1. Open Chrome, then "switch to firefox and go to github.com" (Chrome + Firefox both
     live). Then: "close the two browsers you just opened" -> PASS: BOTH close, response
     names them, NO "edge isn't open", no guessed engine.
  2. "close all browsers" / "close both browsers" / "close every browser" -> same: all close.
  3. Ambiguous singular: with two engines open, "close the browser" -> closes the ACTIVE
     one only (close_engine), NOT all.
  4. No-op: with nothing open, "close the browsers" -> "No browsers were open." (friendly,
     no red FAIL rail).
  5. Single named still works: "close edge" -> close_engine, others stay alive.

Code-level proof (executor + routing are deterministic; brain phrasing is physical):
  uv run pytest -q tests/test_browser_close_engines.py
  -> close_all_engines closes every open engine (registry emptied, _active cleared) and
     names them; single-engine case has no " and "; zero-engine case is a success no-op
     ("No browsers were open"); the handler routes close_all_engines to it and does NOT
     auto-start a browser first (close before the auto-start guard).

-----------------------------------------------------------------------
§43 — clock-anchored recurring reminders fire ON TIME, not 30 min late (A+B1+B2)
-----------------------------------------------------------------------
Background (sweep find): "remind me every day at 1:18pm to check email" correctly created
a cron workflow (schedule "18 13 * * *") and fired at 13:18 — BUT the step was stored as
the NL string "set a reminder to check email" (no time; the time went to the cron
schedule). At fire time that string re-parsed through the brain -> reminder_task/set_reminder
with the 30-min DEFAULT, so the 1:18 fire scheduled a SECOND reminder for 1:48 (30 min late,
daily). The re-parse-each-run is general+accepted; the bug is the lost timing for the
clock-anchored reminder case.
Fix — the cron `schedule` owns the timing, so a reminder step fires immediately:
  A (prompt): CLAUDE.md rule — for "remind me every day at TIME", emit create_workflow with
    the cron schedule AND a CONCRETE reminder step {reminder_task/set_reminder,
    message:<msg>, delay_seconds:5} (concrete steps don't re-parse), message only (no time).
  B1 (handler): in create_workflow, when a `schedule` is present, clamp any concrete
    set_reminder step's delay_seconds to <=5 (drift-proof against a big brain-emitted delay).
  B2 (handler): in create_workflow, when a `schedule` is present, rewrite a reminder-ish
    STRING step ("set a reminder to check email") into a concrete set_reminder{message,
    delay:5} — closes the string-step escape at persist time without cross-layer plumbing.
  Deferred C: fire-time scheduled-flag plumbing through 4 layers — not needed; A+B cover it.
Untouched: one-shot reminders, general string re-parse, the cron scheduler, morning_routine.

PHYSICAL TEST (Part A is model-driven -> verify live; B is deterministic -> unit-tested):
  1. "remind me every day at <~2 min from now> to drink water" -> check data/workflows.json:
     the stored step is CONCRETE ({reminder_task/set_reminder, message:"drink water",
     delay_seconds:5}), NOT a string. At the cron minute it speaks+toasts ~immediately
     (~5 s), NOT 30 min later. DELETE the test workflow after.
  2. Regression: "remind me in 2 minutes to stretch" (one-shot) still fires in ~2 min.
  3. Regression: morning_routine still runs (its non-reminder string steps untouched).
  4. Drift: a scheduled workflow whose reminder step has delay_seconds:1800 -> clamped to 5.

Code-level proof (B is deterministic):
  uv run pytest -q tests/test_scheduled_reminder_steps.py
  -> reminder-string message extraction (strips the time tail); non-reminder strings not
     matched; a string reminder step is rewritten to a concrete set_reminder{delay 5}; a
     concrete set_reminder delay 1800 is clamped to 5 (small/missing delays preserved/
     defaulted); non-reminder steps untouched; create_workflow normalises ONLY when a
     schedule is present (no-schedule leaves the string as-is).

§44 — code_execution: a suggested fix after a FAILED command no longer fakes success (D2)
-----------------------------------------------------------------------
Background (sweep find, fake-success #1): "run python that divides 1 by 0" -> print(1/0)
errored ([ERR 1]). The post-execute layer offered a Sonnet "fix" (python -c "print('Division
by zero is undefined')"); the user confirmed it; the fix exited 0 and its _ok OVERWROTE the
failed command's outcome -> the SYS_LOG row showed GREEN/99%/success for a command that
actually failed (plus a phantom 2nd output + a duplicate "Done. Output:" headline).
Root cause: the row rail is purely result["success"] (_ok=green / _err=red); the confirmed
fix's _ok(out) became the handler's final result. (99% is the brain's ROUTING confidence,
decoupled from execution -- unchanged, existing design.)
Fix (D2, "the user's command owns the row") -- two surgical sites in core/handlers/code_exec.py:
  Edit 1 (_ai_command_confirmation._run_confirmed): when confirm_type=="fix", return on the
    FAIL rail even if the fix exits 0 ("your command failed - the fix ran, but the original
    did not run"). Other confirm types (ai_command NL-translation, plans) ARE the user's
    intended command -> their success stays a genuine _ok.
  Edit 2 (_attempt_fix auto path, autorun ON): a fix that exits 0 now returns _err (same
    framing), mirroring the confirmed path so the rule holds regardless of the autorun toggle.
Duplicate "Done. Output:" headline: gone as a side effect -- that headline only renders on the
success rail, so flipping to _err removes it; the raw fix output already streamed once (fine).
D1 DEFERRED (offering a nonsense fix for an intentional error like divide-by-zero) -- separate item.
Untouched: whether a fix is OFFERED, NL-translation success, plan execution, run_python
first-use gate, the brain.

PHYSICAL TEST (this is the gate -- a green suite alone does NOT close it):
  1. "run python that divides 1 by 0" -> confirm the offered fix -> the SYS_LOG row goes
     RED/FAIL (not green). The user's print(1/0) failure owns the outcome.
  2. Regression: a normal successful command (e.g. "run git status") still shows GREEN.
  3. Regression: an NL-translated shell command you confirm+run still shows GREEN on success.

Code-level proof:
  uv run pytest -q tests/test_code_exec_fix_outcome.py
  -> confirmed fix (exit 0) reports success=False; confirmed fix (exit 1) reports False;
     an ai_command confirmation (exit 0) STAYS success=True (regression guard); the auto-fix
     path (autorun ON, fix exit 0) reports success=False.

§45 — tell_time answers ANY timezone natively (no more code_execution/pytz fallback)
-----------------------------------------------------------------------
Background (sweep find, Priority 2 / cross-cutting theme): "time in Tokyo?" had NO
jarvis_meta path -- tell_time was local-only (datetime.now()) -- so the brain fell back to
code_execution and wrote `import pytz ...` -> ModuleNotFoundError -> an offered `pip install
pytz` fix whose SUBPROCESS hit the 60s stream cap (the "timed out after 60s"; run_python
itself caps at 30s, so the 60s was never the import -- it was the fix). Two issues, one root:
the handler didn't cover the timezone variant. Covering it removes the code path, the pip,
and the 60s hang all at once.
Fix -- give tell_time zone support via the stdlib `zoneinfo`:
  A (prompt, Claude.md §12): "time in <place>" -> jarvis_meta/tell_time with
    parameters.location, PREFER the IANA zone (e.g. "Asia/Tokyo"); do NOT route to
    code_execution. Bare "what time is it" stays local (no location).
  B (handler, core/handlers/meta.py): tell_time reads optional `location`. Resolve IANA-first
    (ZoneInfo(location)), then a curated ~25-city map for a bare city name, else an HONEST
    error ("I don't recognize the timezone for ...") -- never a silent local-time substitute.
  DEP: added `tzdata` (official, pure-Python IANA db). REQUIRED on Windows -- zoneinfo has no
    system tz database there, so without tzdata every ZoneInfo(...) raises and the feature
    can't work. `uv add tzdata` (tzdata==2026.2). This corrects the plan's "zero-deps" premise.
Untouched: tell_date (still local), code_exec.py (the fix mechanism is fine -- we just stop
routing time queries into it), the 60s fix-path cap.

PHYSICAL TEST (Part A is model-driven -> verify live):
  1. "what time is it in Tokyo?" -> routes to jarvis_meta (NOT code_execution), answers
     INSTANTLY (e.g. "2:03 PM in Tokyo"), NO confirm card, NO pip, NO 60s hang.
  2. "time in London" / "New York time" / "Sydney time" -> correct zone, instant.
  3. Regression: "what time is it?" -> still LOCAL time.
  4. Unknown: "time in Narnia" -> honest "I don't recognize that timezone", NOT a fake.

Code-level proof:
  uv run pytest -q tests/test_tell_time_zone.py
  -> local unchanged (no location suffix); IANA zone (Asia/Tokyo -> "... in Tokyo");
     multiword IANA (America/New_York -> "... in New York"); bare-city fallback (tokyo);
     unknown zone -> success False + honest error; blank location -> local.

§46 — search_web grounds follow-ups in the RESULTS, not the model's training (fake-success #2)
-----------------------------------------------------------------------
Background (sweep find, Priority 3 / accuracy): "search for latest nba final mvp 2026" opened
the Google SERP fine, but the follow-up "yes briefly" routed to jarvis_meta/conversational and
the brain SPOKE "Shai Gilgeous-Alexander took the 2026 Finals MVP" -- unsourced, from TRAINING,
and WRONG (that's the 2025 MVP; 2026 = Jalen Brunson). When the user forced "read the top
result" it grounded on Wikipedia and answered correctly (Brunson) -- proving the grounding
channel works; search just never fed it.
Trace: _do_search_web (app_launcher.py) only NAVIGATED -- it never read the results or called
_set_page_cache. The brain grounds follow-ups via get_page_cache() injected as <page_content>
(brain.py), but that cache is set ONLY by read_page/OCR/vision, never by search. So after a
bare search the cache is stale/empty and the brain fills its `response` from training; the
conversational handler returns cache-or-empty, so the brain's (dated) line gets spoken.
Fix (auto-read + prompt rule, Google only):
  A (structural, app_launcher.py): after a successful GOOGLE navigate, browser.read_page() the
    SERP into _set_page_cache -- the SAME channel read_page uses -- so the next turn is
    grounded. Best-effort (a read failure never fails the search). youtube/github/etc. are
    navigational -> NOT auto-read (no latency hit, no spoken summary wanted there).
  B (prompt, Claude.md §3): when a <page_content> block is present after a search, answer
    current/dated facts FROM it, not from memory; never state a post-cutoff fact (future/recent
    year, "latest", "who won 2026", standings, prices) as certain from training; if the answer
    isn't in the results, say "let me read the top result" (route read_page) instead of guessing.
    Pre-cutoff stable facts ("who won the 2022 World Cup") may still come from knowledge.
Untouched: the <page_content> runtime preamble in brain.py (governs ALL page uses -- left as-is
to avoid touching the general path; the §3 rule covers search); the actual MVP is NOT hardcoded.

PHYSICAL TEST (Part B is model-driven -> verify live):
  1. "search for latest nba finals mvp 2026" -> SERP opens; terminal shows "results cached for
     follow-up". Then "summarize briefly" / "who won" -> the spoken answer matches the RESULTS
     (Brunson), NOT a dated 2025 answer, and does not sound confidently unsourced.
  2. A current-fact search where the SERP snippet has the answer -> answer comes FROM the page.
  3. If the results don't contain the answer -> JARVIS offers to read the top result, not a guess.
  4. Regression: "search cats on youtube" -> still just opens YouTube (no auto-read, no summary).
  5. Pre-cutoff: "who won the 2022 world cup" -> still answered correctly (Argentina) from knowledge.

Code-level proof (Part A is deterministic):
  uv run pytest -q tests/test_search_grounding.py
  -> google search reads the SERP into the cache; youtube + github do NOT auto-read (cache
     untouched); a read that raises does NOT fail the search (cache untouched, search ok).

§47 — search grounding pass 2: synthesize (don't raw-dump) + page-cache lifecycle
-----------------------------------------------------------------------
Background (physical test of §46 surfaced TWO regressions):
  BUG A (raw dump): "summarize briefly" after a search read the LITERAL scrape aloud
    ("--- Tab --- / Document title / --- Page content ---") instead of a synthesized answer.
    Cause: jarvis_meta/conversational returned the raw page cache (meta.py), and
    response_composer's conversational branch prefers handler output over the brain's resp
    -> the brain's OWN synthesized (grounded) answer was discarded for the raw dump.
  BUG B (stale-cache leak): after "search cats on youtube" (correctly no auto-read), the
    unrelated "who won the 2022 world cup" got the SAME OLD NBA page back verbatim. Cause:
    the page cache NEVER expired or cleared -- it lived forever and leaked into any later
    conversational turn (a NEW wrong-answer class: topically wrong, not just dated).
Fix:
  A (meta.py): conversational returns EMPTY -> the composer falls through to the brain's
    resp, which the brain writes FROM the injected <page_content> (synthesis, not a dump).
  B (shared.py): page cache gets a timestamp + 120s TTL (get_page_cache auto-expires) and a
    new _clear_page_cache(). Cleared on a NEW search (app_launcher _do_search_web, ALL
    platforms) and on browser navigate (browser_handler) -- topic-change signals. A same-page
    multi-turn follow-up still works within the TTL.
  brain.py: <page_content> cap 600 -> 1200 so synthesis has headroom (safe now that stale
    caches expire + clear).
Untouched: the grounding MECHANISM from §46 (google auto-read) -- that content was already
correct (Brunson was in the cache); §47 fixes PRESENTATION (synthesize) + LIFECYCLE (expire/clear).

PHYSICAL TEST (Part A synthesis is model-driven -> verify live):
  1. "search latest nba finals mvp 2026" then "summarize briefly, team + mvp" -> a SYNTHESIZED
     spoken answer (e.g. "Jalen Brunson, Knicks beat Cavaliers 4-0"), NOT the raw scrape.
  2. Then "search cats on youtube" (clears cache), then "who won the 2022 world cup" ->
     Argentina from knowledge, NOT the stale NBA dump. (The core BUG-B repro.)
  3. Wait >2 min after a search, then an unrelated conversational question -> no stale leak (TTL).
  4. Regression: same-page double follow-up within 2 min still grounded (both answer the page).

Code-level proof:
  uv run pytest -q tests/test_page_cache_lifecycle.py tests/test_search_grounding.py
  -> cache returns within TTL / None after TTL (entry dropped); _clear_page_cache clears;
     conversational returns "" even with a cache present (no raw dump); google search reads
     the SERP into the cache; youtube + github CLEAR a stale cache and do not auto-read; a
     failing read doesn't break the search (cache left cleared).

§48 — settings actually persist/apply: set_wake_word + change_theme (fake-success #3 & #4)
-----------------------------------------------------------------------
Background (sweep Priority 7): two settings actions claimed success but didn't take effect.
  7b set_wake_word: 'set it as STARK' -> '(restart required)', but after restart the wake word
    was STILL 'jarvis'. The WRITE was fine (config.save persists wake_word); the bug was
    config.load() overriding it from the environment. TWO-PASS: (1) first removed wake_word from
    the generic env-overlay loop; (2) re-test STILL failed -> ground truth: .env contains
    WAKE_WORD=jarvis (the app loads .env), so an 'explicit env wins' branch re-clobbered the
    saved 'stark' at load, and the NEXT save() (a theme change) wrote 'jarvis' back to
    jarvis.json. A read-back/precedence failure, not a write failure.
  7a change_theme: 'switch theme to matrix' -> 'going green / applied', but the theme NEVER
    changed. The handler returned _ok WITHOUT persisting config.theme or applying anything --
    a pure fake. (Startup DOES read config.theme; theme follows the wake-word convention:
    persist + restart. Live in-session re-theming is a separate parked refactor.)
    Also: Claude.md advertised gold|cyan|emerald|crimson but the REAL palettes are
    cyan|teal|amber|indigo|matrix -- 3 of 4 documented names didn't exist (silent fallback).
Fix:
  7b (config/settings.py): drop wake_word from the generic env-overlay AND make jarvis.json
    AUTHORITATIVE -- .env WAKE_WORD only SEEDS a fresh install (if w and 'wake_word' not in data).
    Survives restart even with a leftover WAKE_WORD=jarvis in .env. NOTE: the already-saved
    jarvis.json still holds the clobbered 'jarvis', so re-run 'set wake word to stark' ONCE
    after this fix; it then sticks.
  7a (meta.py): change_theme validates against the real palettes (+ aliases gold->amber,
    green/emerald->matrix, blue/purple->indigo, turquoise->teal), persists config.theme +
    save() (rollback on failure), and returns an HONEST 'set to X -- restart to apply'. An
    unknown theme is an honest error, not a silent fallback. response_composer speaks the
    handler's line (like change_voice) so the honest message is what's said, not the brain's
    optimistic 'going green'. Claude.md §12 theme names corrected + aliases documented.
  Sibling audit: change_voice is REAL (switch_tts_voice persist=True + ok check) -- not fake.

PHYSICAL TEST:
  1. 'set wake word to stark' -> honest 'restart to pick it up'. RESTART -> Settings shows
     'stark' AND the listener uses it (no revert to 'jarvis'), DESPITE .env WAKE_WORD=jarvis.
     The core 7b repro (needs the re-set noted above, since jarvis.json currently holds 'jarvis').
  2. 'change theme to matrix' -> 'set to matrix -- restart to see it applied'. RESTART -> the
     HUD is actually matrix (green). Settings theme shows matrix.
  3. 'change theme to gold' -> accepted as amber (alias); restart -> amber HUD.
  4. 'change theme to chartreuse' (unknown) -> honest 'not a theme I have; options: ...',
     NOT a fake success, theme unchanged.

Code-level proof:
  uv run pytest -q tests/test_settings_persistence.py
  -> persisted wake_word survives load() with no WAKE_WORD env; persisted value WINS over a
     .env WAKE_WORD=jarvis SEED (the real repro); env seeds ONLY when jarvis.json has no
     wake_word key; default 'jarvis' when neither; change_theme valid persists + says 'restart';
     alias gold->amber; unknown 'crimson' -> honest error, theme unchanged, never saved;
     a save() failure rolls config.theme back.

§49 — vision_analysis runs OFF the Qt main thread; ctrl+shift+r no longer freezes the UI
-----------------------------------------------------------------------
Background (sweep Priority 5): the ctrl+shift+r hotkey (read_screen) did NOT read the screen
AND froze the UI, while the TYPED 'what's on my screen' worked. Trace: the hotkey dispatches
vision_analysis/describe (backend_signals_mixin._on_hotkey) -- the SAME path as the typed
command -- and vision_analysis ran SYNCHRONOUSLY on the Qt main thread (execution_mixin: only
code_execution + off-thread-safe workflows used the worker). Its blocking Anthropic Vision API
call (vision.analyze_image) stalled the event loop -> UI freeze. The hotkey just made it
obvious (fired from another app, nobody watching the frozen window). The capture is mss/PIL
(thread-safe, no Qt), so it's safe off-thread.
Fix (execution_mixin._execute_result): route direct vision_analysis dispatch through the SAME
_spawn_code_worker harness as code_execution (result returns via code_execution_done ->
_on_code_execution_done -> _finish_execute; vision_analysis is in _ACTION_INTENTS so the
analysis is spoken correctly). EXCEPTION: source='browser' uses vision.capture_browser_page()
-> Playwright (thread-affine to the main thread), so that ONE source stays synchronous.
Scope: vision_analysis only (read_screen OCR is local/fast, not the reported freeze).

PHYSICAL TEST (threading -> physical gate):
  1. Press ctrl+shift+r (from JARVIS OR another focused app) -> the UI STAYS RESPONSIVE
     ('Analyzing the screen...' toast), then speaks the description. NO freeze.
  2. Typed 'what's on my screen' -> same: responsive, then the description. NO freeze.
  3. 'describe the browser page' (source=browser) -> still works (synchronous, Playwright).
  4. During a vision read, the HUD/other buttons remain clickable (proof it's off-thread).

Code-level proof:
  uv run pytest -q tests/test_vision_offthread.py
  -> vision (source=screenshot) routes to _spawn_code_worker, NOT inline dispatch; a missing
     source defaults to screenshot -> off-thread; source=browser stays SYNCHRONOUS (dispatched
     inline on the main thread, no worker).

-----------------------------------------------------------------------
Regression sweep (run after all of the above)
-----------------------------------------------------------------------
  - "what time is it" / "open chrome" / "take a screenshot"  -> all still work.
  - Set + cancel a reminder normally.
  - Run a normal workflow end to end.
  - Confirm a normal destructive action with a plain "yes" -> it executes.

Automated baseline (from repo root):
  uv run pytest -q -m "not integration and not qt and not slow"
  -> expect all green (166 passed, 2 deselected at time of writing).
